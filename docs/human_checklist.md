# 坐下来按这个顺序看

2026-08-04 那一长轮攒的，**2026-08-05 晚上大改了一次**——那天修掉的一个根因（倍率因子从未进曲线）把下面**两条长期悬案**变成了「重测一遍就知道」。

按「坐下来先做哪个」排的，不按重要性。

---

## ⚠ 2026-08-10 夜跑：三把枪**每一格**都失败，等你回来看

`m249` 3/3、`mg3` 1/1、`vss` 1/1 —— 全灭。同一轮里 mp5k 8/8、js9/tommy/ump45 14/14，所以**不是管道坏了**。

```
123 格起跑   100 usable   23 FAILED   1 次 HALT
失败分类     18 agree     5 state
```

**它们是这一批里后坐力最大的三把**（两挺机枪 + 一把 DMR），视角走得最远。但这只是形状，不是结论——我**没有**把 23 次失败按 agree/state 逐条对上，只有分类总数。

要看的证据都在：

```
calibration/artifacts/nights/night_20260810_*/fail_m249_*
calibration/artifacts/nights/night_20260810_*/fail_mg3_*
calibration/artifacts/nights/night_20260810_*/fail_vss_*
pixi run night --rejudge calibration/artifacts/nights/<ts>      # 离线重判，不用再开枪
```

⚠ **样本一梭没丢**（库永不删除），所以这三把可以纯离线查完再决定要不要补打。

两个已知的、可能相关的东西：

- `mg3` 有**两个自动档，cyclic 差 1.50 倍**，而 `ensure_fire_mode` 存在但采集路径曾经一次都没调过。这一轮调没调、存的是哪一档，`fail_mg3_*` 里能看出来。
- `vss` 用集成镜 `vss_pso1`，而它是 `ads_end=False` 比例最高的枪之一（83.7%）。它的 `agree` 失败可能跟开镜读数不可信是同一件事。

---

## 顺序

| 序 | 事 | 你要花 | 解锁什么 |
|---|---|---|---|
| ~~1~~ | ~~盯屏看 `strip` 丢枪~~ | — | **✅ 已解决 2026-08-04**（根因在 `SlotDetector`，见下） |
| ~~2~~ | ~~试 `scope_4x` 能不能装 mp5k~~ | — | **✅ 已测 2026-08-05**，装不上，已进 `EXCLUDE` |
| **1** | **重测 4x 的拒绝率**（**一个预测等着被证伪**） | 15 分钟机时，你不用看 | 同时结案原第 5、6 条 |
| **2** | 定 `MARGIN_MIN` | 5 分钟读表 | 无（现状可用，只要你认一下） |
| **3** | 定 `training_data/` 进不进 git | 1 分钟 | 新克隆能不能读顶栏 |
| **4** | 定 `GRIP_FACTOR` 改不改 | 5 分钟读表 | 直接改游戏里的压枪行为 |
| 5 | `tactical_stock` 的 25σ 矛盾 | 长 | 理解模型在测什么 |

---

## 1 — 重测 4x 的拒绝率　⏱ 15 分钟机时，**有一个明确的预测**

**这条同时吃掉了旧清单的第 5 条（镜子 20% 拒绝率）和第 6 条的后半（scope 正交性）。** 理由是它们很可能**根本不是两个谜，而是同一个已修 bug 的两个症状**。

旧数据长这样：

```
剖面      patch   拒绝率            mad
red_dot     7     3.5–11.3%        0.3–0.9    （19 格）
4x          5     18.6–22.5%       2.09–4.13  （4 格）
```

**那 4 个 4x 格全部采于 `build_weapon` 从来不设 `scope` 的时代。** 后果链条在 `calibration/CLAUDE.md` 里已经写死了：补偿只发 1/4 → 残差 +265% → **一梭把视角推 2692 counts**，而参考图块的无歧义范围只有 **68**。视角每梭跑出量程 40 倍，相关器当然拒帧。

而红点档 `scope_factor = 1.0`，**不受这个 bug 影响**——所以「红点 3.5–11.3% / 4x 18.6–22.5%」这个对比，测的很可能就是补偿错没错，不是镜子难不难跟。

**预测（未测，这就是要跑的）：修完之后 4x 的拒绝率掉进红点档那个区间。**

- **掉了** → 第 5、6 条同时结案，「4x 的 patch 太少 / 相位相关量程不够」那一串假设根本不用再想，scope 正交性得**重测**（旧的 `comp_ar 4x 0.5625 ± 0.0729` 作废）
- **没掉，还是 20%** → 那才是真的没有便宜假设了，回到旧第 5 条那套（我把每个 patch 的裁图逐帧存下来给你翻）

```bash
pixi run python calibration/harvest.py --weapons aug --configs bare,comp_ar \
  --sight scope_4x --mags 6 --bare-mags 6 --home \
  --out docs/recoil/runs/scope4x_recheck.jsonl
```

**aug 是对的选枪**：它今天实测能装 4x（`red_dot -> Upper_ACOG_01_C`，一次右键），而且红点档已经有 `comp_ar` 的基准（0.7747 ± 0.0086）可以对照。

⚠ **别用 mp5k**——它装不上 4x，见下。

---

## 2 — `MARGIN_MIN` 取哪档　⏱ 5 分钟

现在是 **1.25**。代价表在 `detector/attachment_detector.py` 的常量注释里，摘要：

```
floor   正确读数保住   冒名被拒
1.05      100.0%        6.6%
1.10       99.1%       13.3%
1.25       98.3%       27.6%      ← 现在
2.50       94.9%       83.7%
5.00       92.0%       92.1%      ← 再高只亏不赚
```

**这个闸赔掉的 33 次全部是 `supp_ar`**（DMR 上对 `supp_sr`，最薄 margin 1.05），占语料 1.7%。而且**拒答不引发错误动作**：`'?'` 走 `verifiable=False`，`ensure_kit` 报「证不了」不重试、不重拖。

### 活体实例（2026-08-05，人念的件名做真值）

mk14 刷出来自带的枪口，`loadout()` 连读三次都是 `'?'`。按出货裁图（`HUD_REGIONS['att_1_muzzle']`，63×63，`prefer='solved'`）实测：

```
Muzzle_Suppressor_Large_C          21.2   ← 人念的是「消音器 突击步枪」= 就是它
Muzzle_Suppressor_SniperRifle_C    23.6
Muzzle_FlashHider_SniperRifle_C   963.1   ← 第三名，远 45 倍
                                   margin = 1.11 < 1.25 → '?'
```

**检测器的第一名是对的**（人念的名字证实），只是 1.11 过不了 1.25。降到 1.05 这一格当场读对。

⚠ **它不是「缺模板」**，虽然长得一模一样。我一度把它列进【待采模板】和「mk14 cheek_pad 的阻塞项」，**两条都是错的**——两根灰管子再采一百张模板也还是一样，而 cheek_pad 那两格本来就要裸枪口，卸掉就没这回事。

### 取舍的真正形状

| floor | 失败时会怎样 |
|---|---|
| **1.25** | 拒答（`'?'`）→ `verifiable=False` → 调用方不动作。**安全** |
| **1.05** | 可能**报出错误的件名**。而错名是「真分歧」，会触发重试重拖 |

对**后坐力**这条线，认错影响约等于零——`supp_ar` 和 `supp_sr` 实测都是 null（akm 1.007 / m416 0.989 / mp5k 1.011 / vector 1.034）。对**装配校验**这条线，错名比拒答贵。

**我的建议是保持 1.25**，你只要认一下，或者说一句降到 1.05。

---

## ~~3 — `training_data/` 进不进 git~~　✅ 已定：**开白名单**（2026-08-05）

这条是「**代码能不能跑**」，不是「结论要不要历史」。⚠ **后者 2026-08-08 变了**：语料搬去 `calibration/artifacts/`，`docs/` 现在是跟踪的。

2026-08-05 起 `detector/lobby_nav.read_page` 从「按位置认名」改成「按字形认名」，于是它依赖 `training_data/pubg_assets/lobby/tabs/` 下的模板。`training_data/` 整个被 gitignore，所以新克隆的仓库读不出任何语言的顶栏，`ensure_in_match()` 整条链拒绝动作。

**已开白名单**：`36 个文件 / 44 KB`（en 12 + en_alt 12 + zh 12）。
⚠ 这里原来写的是「24 张、30 KB」——漏算了 `en_alt` 那一组，它是留出验收里最差胜者 0.737 对亚军 0.587 时补的第二种渲染。

**判据是「干净克隆能不能跑」，不是「文件小不小」。** 语料大且可重建，这些小且**不可**重建（重建要语料，而语料本身不进 git）。2.9 MB 的 `weapon_hud_bank.npz` 因此仍在外面——它能用 `calibration/build_weapon_hud_bank.py` 从语料重建。

⚠ 踩到一个坑：只给**目录**开白名单不够，`.gitignore` 第 10 行的 `*.png` 在任何深度都匹配，会把文件原样挡回去（目录放行了，文件没有）。**re-include 是按规则的，不是按树的**，得再写一条 `!.../tabs/*.png`。查是哪条规则在拦：`git check-ignore -v <文件>`。

---

## 4 — `GRIP_FACTOR` 改不改　⏱ 5 分钟

`detector/weapon_attachments.py` 那张表**直接改游戏里的压枪行为**，所以这是你的事不是我的。

2026-08-05 在 m762 上一件一格实测（每格自带同轮裸格，`--home`）：

| 握把 | 实测（m762） | 表里在用 | 差 |
|---|---|---|---|
| `vert_grip` | 0.7723 ± 0.0234 | 0.85 | −9% |
| **`thumb_grip`** | **0.7847 ± 0.0094** | **0.95** | **−17%** |
| `tilted_grip` | 0.8169 ± 0.0210 | 0.809 | ✅ 对上（0.4σ）|
| `half_grip` | 0.8565 ± 0.0235 | 0.92 | −7% |
| **`light_grip`** | **0.8748 ± 0.0087** | **1.0** | **−12.5%** |
| **`laser`** | **1.0058 ± 0.0099** | 1.0 | ✅ **干净的 null**（0.6σ）|

三条值得看：

- **拇指握把几乎和垂直握把一样好**（0.785 vs 0.772），而表里说它差得多（0.95 vs 0.85）。因为那张表避开拇指握把是白避的。
- **轻型握把不是「什么都不做」，它压 12.5%。** 表里写 1.0，所以装它的时候压枪**一直少乘一个系数**。
- **`laser` 是真 null，而且带阳性对照**：同一把枪、同一个握把槽、同一套测法，`vert_grip` 交出 0.772。所以「激光不影响后坐力」现在是实测结论，不是假设。**一个 null 只有在同条件下有个非 null 才可信**，这是这批数据里唯一满足这条的。

排序（好→差）：`vert 0.772 ≈ thumb 0.785 < tilted 0.817 < half 0.857 < light 0.875 < laser 1.006`。**表里的排序和实测不一样。**

⚠ 全部只在 **m762** 上测。跨枪唯一复现的是 `tilted_grip`（m762 0.8169 对 mp5k 0.809）。而这个项目已经反复测到**单槽因子不跨枪通用**（`comp_smg` 在 mp5k 0.594 / vector 0.711），所以「改表」这个动作本身可能就是错的抽象——正确的可能是**按枪存**。

---

## 5 — `tactical_stock` 的 25σ 矛盾　⏱ 长

官方 −20.00% Recoil Pattern Scale，实测 **1.00 ± 0.01**（三格两枪），**~25σ**。

「pattern scale 不是我们测的量」这个解释**已被否**——三个握把同样标称 −20% pattern，交出 0.809 / 0.835 / 0.761；同槽的 `heavy_stock` 交出 0.835（m416 上 0.8165 ± 0.0189，9.7σ，是这条轴的阳性对照）。所以那个字段就是我们在测的，**只有这个件对不上**。

这条没有便宜假设，也没被今天的 scope 修复碰到（枪托格全是红点档测的）。

---

## ✅ 已结案（保留理由，别再走一遍）

### `strip` 把整把宿主枪丢到地上 —— 2026-08-04

根因**在读数器不在拖拽**：剥光的 AKM，它自己的弹匣填满 magazine 贴片，395 个 Canny 边缘对着阈值 120 → 那个槽**永远读 `filled`** → `strip` 对着空槽再拉一次 → 空槽上的手势打到底下的武器行 → 整枪落地。11 个 run、74 次。

判据换成**正向识别配件**（`filled ⟺ 认出一个件`）。详见 `detector/CLAUDE.md`。

### `scope_4x` 装不装得上 —— 2026-08-05，答案分两半

| | 结果 |
|---|---|
| **aug** | **装得上**，`red_dot -> Upper_ACOG_01_C`，一次右键 |
| **mp5k** | **装不上**，已进 `EXCLUDE` |

**mp5k 那条推翻了目录里昨天写下的一句话。** 昨天写的是「mp5k/scope_4x 那两次是本项目的 bug，已澄清」——不是。判决靠的是**同场阳性对照**，3.5 分钟内、同一条代码路径、同一个槽、同样的起始状态（刷新器自动配上的红点）：

```
21:07:16  aug  + scope_4x   red_dot -> Upper_ACOG_01_C   ok     journal 489
21:10:58  mp5k + scope_4x   red_dot -> red_dot           MISS   journal 490
21:10:59  mp5k + scope_4x   red_dot -> red_dot           MISS   journal 491
```

而 `pixi run drag-log` 说两次失手的**手势都是干净的**（三个字段的实际读数在 `control/CLAUDE.md` 那一节）。**手势干净而游戏不接 = 拒绝。**

⚠ 由此多出**第三种失败签名**：`reads <旧件>`（游戏拒绝），并列于 `reads ''`（根本没落位）和 `reads '?'`（落位了但认不出）。**它最像本项目的 bug，而分辨它需要同场对照**——不是重读日志，重读日志正是产出那句被推翻的话的办法。

### `vector / grip / angled_grip` —— 是个幽灵条目

`angled_grip` 在 2026-08-04 就被改名成 `tilted_grip` 了（`attachment_catalog` 里有 ALIAS），刷新器 roster 里**根本没有这个键**，所以它永远「装不上」。真正的不兼容（vector 不收 `tilted_grip`）2026-08-04 就已经手工确认并进表了。

**清单上的键名和代码里的键名会各自漂。**

### `factor_model.md` 进不进 git —— 不进（2026-08-05）

⚠ **这句已经不成立**——`docs/` 现在是跟踪的，这份 checklist 也有版本历史了。经过和现状归 `calibration/CLAUDE.md`「产物往哪写」那一节持有。

### vss 的一切 —— 根因不是 vss，是 `build_weapon` 从来不设 `scope`

`Weapon.set_seq` 里 `factor = scope_factor * naked_scale * att_f * posture_f`，而 `scope_factor` 只有 `set('scope', …)` 会写。`build_weapon` 设 name / posture / muzzle / grip——**没有 scope**。所以采集台在任何倍率下都在发**红点的曲线**。

| | 曲线（发出去的） | 实测真值 | 残差 |
|---|---|---|---|
| aug 红点 | 1741 | 1812 | **+4%** |
| aug **4x** | 1741（**该是 6964**） | 6347 | **+265%** |

vss 自带的 PSO-1 在 `_SCOPE_TO_MAG` 里就是 4x，所以它一整天的「+111% 残差」同理——比值 3.26，接近 4。**为它试了 8 次、否掉 7 个解释、写了一整节文档，全部建立在那个误读上。**

---

## 附：今天冒出来、**没解释**的（2026-08-05）

不是待办，是**观察**。放这儿因为它们各自可能是别的东西的线索。

**1. vss 的姿势图标只有 4% 的帧画出来。**
同一轮日志里 `crosshair 78% (decides) / posture icon 4%`。而同一天在 m416 上实测：开镜时图标 **3786/3787** 可读。没查。⚠ 这条现在**可能也是倍率的事**——vss 恒 4x，而 4% 这个数是在补偿只发 1/4 的那一轮记的，视角一梭跑 2692 counts，图标未必还在它该在的位置。**重测第 1 条的时候顺手记一下 aug@4x 的图标可读率**，就知道了。

**2. ADS 占比闸门可能对射速快的枪有系统偏见。**
vector 丢弹匣全部卡在 **79–80%**（闸门 80%），而 `muzzle` 那格是 mag 0/1/2 全丢、3/4/5 全干净。
猜想（**未验**）：每梭开头有一段**固定时长**不在开镜。vector 一梭 2.41 s，480 ms 就是 20%；m416 一梭 4.13 s，同样 480 ms 只有 12%，正好在闸门里侧。若成立，**不是 vector 更差，是同样的绝对损耗除以更短的弹匣**。验法：在**开火期间**采 ADS 读数，而不是只采静止帧——`capture_ads` 现在采的全是静止的。

**3. 一个「跨枪一致」的结论，在误差棒收紧后翻成了「不一致」。**
`heavy_stock` 在 vector 的格只有 2–5 个弹匣时是 **0.7σ 一致**；补到 6 个之后变成 **3.0σ 不一致**。反方向极少发生。**短格上得到的一致性不要当结论。**

**4. 右键装配在弹匣槽上只有 26%，别的槽 100%。** ← **2026-08-05 有人盯屏重做，孤立地做是 6/6**

`inventory 131/131、muzzle 24/24、grip 22/22、stock 18/18、scope 1/1`，而 **magazine 18/68**。

盯屏那轮（mp5k + `ext_smg`，每次读回、每次复位，**人眼和仪表两边都是 6/6**）：第 0 次是**换掉已占用的槽**，成功——当场证伪「PUBG 不给已占用的弹匣槽换装」。`kit_facts` 上那 6 笔 `mp5k magazine ext_smg` 因此是假的，已销账。

⚠ **这让右键和拖拽看起来是同一个形状**：

| | 孤立探针 | 真采集器 |
|---|---|---|
| 面板到面板的拖拽 | 22/24 = 0.92 | ~0.5 ⚠ **假数** |
| **弹匣槽右键** | **6/6** | **18/68** |

### ✅ 两行都结案了，而且**都不是「手势之间夹着的东西」**

当时由这张表推出的「肇因不在手势，在手势之间」是错的，两行各有各的原因：

| | 结案 | 真正的原因 |
|---|---|---|
| **弹匣槽右键** | 2026-08-07 | 重试用**循环外算好的坐标**。第一次其实成功了，把原件顶进背包、整个列表下移，第二下打在移位后的行上把原件装了回来 |
| **面板到面板的拖拽** | 2026-08-08 | **那个 ~0.5 不是失败率，是判据的自述。** 光标偏出 `PLACE_TOL=2px` 就跳过读回，`moved` 留 `None`——而这类记录里 **98% 东西已经落地了** |

拖拽那一行的数字（全库日志复核）：

```
说失败，其实落地了   147   ← 占全部「失败」的 98%
说成功，落地了       104
说成功，其实没落地     53   ← 光标判据根本看不见
说失败，确实没落地      3
```

修法是「**行数是判据，光标降级成诊断**」（`control/inventory.py`）。按天切开：未验证比例 **27%/43% → 1%/0%**，落地率 **52% → 79%**。剩下 ~20% 是真失败，**平坦无模式**，重试能吸收。

⚠ **这张表的右列不是两次测量，是两个坏判据的输出。** 拿它推共同肇因，推出来的必然也是关于判据的——而当时它被当成了关于游戏的。**盯屏确实没用，但不是因为那一下干净，是因为要看的东西不在屏幕上，在记录里。**

**5. 姿势 × 配件的交互，两把 AR 符号相反。**
m416 装了件姿势收益**变小**（+13.8% / +21.6%），m762 **变大**（−6.2% / −7.0%），都在 3.7σ 以上。交互是硬的，**方向不是**。所以不能用一条修正曲线糊过去，而为什么不同没有解释。

**6. 重定时会让一格永远达不到满弹匣。**
`vector/bare` 实测 1164 rpm 于是重定时并丢掉那一梭，但整格最后拟合出来是 **1138.7** 存回去——下一轮又会测出 1164、又重定时、又丢一梭。**每格 5/6 是稳态，不是偶然。**

**7. mk14 是 23 把全自动枪里唯一没有曲线的**（`len=1, sum=0`）。所有 `cheek_pad` 的备选枪里，它和 vss 是仅有的两把全自动 DMR。**它要先换开火模式**。

**8. groza 唯一能装的枪口件是消音器**（已测的 null），所以它**根本没有能压后坐力的枪口**。mg3 更彻底：只有瞄具槽。这两把在配件轴上没有任何可测的东西。

---

## 附二：今晚实测出来的数（都在 `docs/recoil/factor_model.md`）

```
枪口   comp_ar    m416 0.7747/0.7805   ace32 0.7733      comp_smg  mp5k 0.594  vector 0.711
       supp_ar    akm 1.007（两轮）  m416 0.989  groza 0.948*   supp_smg  mp5k 1.011  vector 1.034
握把   vert_grip  mp5k 0.747–0.761   vector 0.815–0.817   m762 0.772
       thumb      m762 0.785         tilted   mp5k 0.809  m762 0.817
       half       mp5k 0.835  vector 0.872  m762 0.857
       light      m762 0.875         laser    m762 1.006（null，带阳性对照）
枪托   heavy      mp5k 0.835/0.836   vector 0.878   m416 0.8165（9.7σ）
       tactical   1.00（恒等，见第 5 条）
托腮板 cheek_pad  vss 0.8161 ± 0.2304（0.80σ，**什么都没确立**）

正交性  握把×枪托  ✅ −1.1σ / +1.2σ        枪口×握把  ❌ +2.7σ / +4.1σ
        枪口×枪托  ✅ +0.7σ / +1.5σ        三向       ❌ +5.5σ / +2.4σ
```

*groza 的裸格只有 1 个弹匣，那个 0.948 不算结论。

**两把枪四条检验逐条同号**：坏的永远是枪口×握把那一条边，而且永远是正的（相乘会**过度补偿 6–10%**）。三向的偏差 ≈ 那条边本身，没有独立的三体项。

所以模型是：

```
factor = muzzle × grip × stock × C(muzzle, grip)
```

`C` 按枪走（mp5k +9.6%、vector +6.0%），跟单槽因子一样不跨枪通用。
