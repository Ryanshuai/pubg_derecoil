# 这个项目的一条法则

每层的规矩在各自的 `CLAUDE.md` 里。这里只放一条,因为它跨层,而且这个项目**为它付过最贵的一次账**。

---

## 修正必须落在偏差被观测到的**那个位置**上

> 第一发到第二发之间的偏差,约束并修正**第一发到第二发之间**的压枪。

不许摊开,不许跨项,不许「顺便把邻居也带一点」。优化器里**只有这一条约束**。

### 为什么

后坐力是**逐发独立**的:第 k 发打没打出去,不改变第 k+1 发的踢腿。所以偏差和修正之间是一对一的线性关系,**没有跨项可言**。

就算游戏里真存在某种跨发耦合,**也不许去拟合它**。那是拿一个 41 维的模型去拟合噪声,而它带来的每一点「改善」都只存在于那一批测量上——**直接过拟合**。

### 它是怎么被破坏的,以及代价

`fit_curve` 曾经在把残差喂回曲线之前先做一次 **5 发宽的移动平均**,当作噪声防御。那一步把第 2 发的偏差摊到了第 0–4 发上——**「往第一发对」**。

后果不是「修正变钝」,是**符号翻转**:

```
误差递推        e ← (I − α·S)·e          S = 那个平滑
盒式滤波的传递函数在锯齿频率上是负的      w=5 时 min H = −0.25
于是               1 − α·S = 1.025 > 1
m416 的 255 次更新                        1.025²⁵⁵ ≈ 540 倍
```

**它不是在压制逐发振荡,是在放大它。** 实测后果:m416 的曲线逐发变成 `+438 / −270` 的锯齿,补偿在一发把准星按下去、下一发往上推 126 counts,屏幕肉眼可见地抖。

而**噪声防御从一开始就不需要**:逐发噪声 sd 2.2–15.9 counts,平均之后只剩 0.3–1.4,对着 56–645 counts 的系统性误差——**信噪比约 500:1。那块磨砂挡的是信号,不是噪声。**

⚠ **参数被删掉了,不是设成 1。** 一个大于 1 就构成正反馈的窗宽,不该以「可配置」的形式存在。

---

## 而「那个位置」是**时间**,不是弹号 —— 规格在 [`MODEL.md`](MODEL.md)

上面那条一个字不用改。变的是「位置」拿什么坐标写。

```
y_comp(t)                     我们自己在 Pico 上排的时刻表,完全已知
y_obs(t)                      每帧一个样本,t = 距**点击**多久
y_true(t) = y_obs(t) + y_comp(t)
```

**要拟合的本来就是一条「压枪幅度随时间」的曲线,而固件播的正是这个。** 中间那层「42 个弹桶」是分析这一层自己加的,枪和固件都不需要它。

> ### ⚠ `MODEL.md` 是主法则。任何地方与它冲突,以它为准。
>
> 这里只留三条推论,因为它们会让人在别处读到相反的话:
>
> - **样本永久保留,每次拟合全量。** 没有 α、没有 EMA、没有轮次——**「这一格有没有收敛」这个问题不存在了**,只剩「样本够不够」。下面关于收敛判据的两节因此只适用于旧回路。
> - **离群的单位是一整梭,不是一个点。** 让一梭报废的东西污染的是整条轨迹,而那条轨迹看起来完全合理。**逐点剔除永远抓不到它**,所以是聚类取最大簇。
> - **弹桶、火循环、`W = 13 ms`、`np.interp` 的边界钳位,在时间坐标下全部不存在**——不是修好了,是不再有这个东西。

**2026-08-08 实现退场了,所以下面标 ⚠ 退场的几节跟着退场。** `analyse()`、`fit_curve`、`harvest` 和整个弹桶路径已经从仓库里删除;`MODEL.md` 第七节写的「跟着实现一起退场」就是这一刻。没标退场的仍然生效。

---

## ⚠ 已退场(2026-08-08):对角约束的可观测性检查

**留在这里是因为它的形状会复发,不是因为它还要做。** 弹桶没有了,所以「第 k 发的位移被记到第 k+1 个桶里」这个失效模式没有指称对象了——两个原点合成了一个,而那个原点是本仓库自己发的点击。

⚠ **但它的替代品必须存在,而且现在存在**:一个拟合**安排不出来**的检查。新的那个在 `harness/verdict.py` 第 4 项——不同强度曲线打的梭,各自加回`y_comp` 之后必须给同一个 `y_true`。拟合器看不见一条梭来自哪条臂,所以它伪造不了一致。**一条臂 = 没验过 ≠ 通过。**

下面是旧的那一个,读它是为了记住为什么需要一个:

一个完美的对角优化器,如果观测本身错位(第 k 发的位移被记到第 k+1 发的桶里),会**忠实地把修正加到错的地方,而且每一轮都自洽**。

这不是假想。这个项目里有两个不同原点:

- **拟合的弹桶**锚在**弹药计数器**上
- **固件播放曲线**锚在**点击**上 + `RECOIL_FIRE_DELAY_MS`

它们之间那个偏移只在 AUG 上量过一次(`W = 13 ms`, n=36),然后用在所有枪上。

**所以每次动这条回路,都要有一个独立的可观测性检查:在第 k 发上放一个已知的尖峰,看哪一发动。** 对角就是好的;动的是 k±1,那么再正确的优化器也收敛不了。`tools/probe_impulse_ab.py` 做的就是这件事(只覆盖第 0 和第 12 发)。**那个探针 2026-08-08 随弹桶一起删掉了。**

**第一发和最后一发是这个检查最有用的两个点**,不是因为它们重要,而是因为它们**没有邻居可以借**:第 0 发之前没有东西能把误差推给它,最后一发之后没有东西能接住。它们测的是回路本身,不是枪。

---

## 判据必须能看见它要管的那个维度

上面那件事之所以活了 255 轮没被发现,是因为**每一个判据看的都是「有符号的整梭残差和」**,而锯齿求和恰好抵消:

```
m416   有符号残差 +64（4%）        逐发误差绝对值之和 783（50%）
```

**同一条曲线,同一批数据,差 12 倍。** 报出来的是 0.9%,而屏幕在抖。

所以现在每一格同时打印这些量,而且都存进记录:

| 量 | 看得见什么 | 看不见什么 |
|---|---|---|
| `residual`(有符号和) | 视角最后停在哪 | 它路上跑了多远 |
| `sum\|per-bullet e\|` **整格均值** | 逐发误差,不会对冲 | **这一格自己有没有在收敛**(见下) |
| `sum\|per-bullet e\|` **首梭 → 末梭** | `--apply` 格的收敛 | 影子格里它只是离散 |
| `wander` | 画面中途最远漂到哪 | 高频抖动 |

**一个自洽、算术正确、而且瞎的判据,比没有判据更危险**——因为没人会去质疑它。这个项目 2026-08-07 一晚上撞了六次同一个形状。

## 而第七次,是那个用来修前六次的判据自己瞎了

加上 `sum|per-bullet e|` 之后**几个小时**,我就用它得出了一个错的结论:

```
m416 bare，13 梭
  mag  0        1068
  mag 12         513          ——  减半了
  整格均值       832
  上一轮均值     783          ——  于是报「它变差了」
```

**它在收敛,而报出来的数在涨。** 因为整格均值是对**一整轮**取平均,而 `--apply` 的一轮里曲线每梭都在被改写:前面的梭打的是坏曲线,后面的梭打的是好曲线,均值落在中间。**拿它跟另一轮的均值比,两边都说明不了任何事。**

由此得出的下一步——「除了滤波器还有第二个正反馈源,回去查」——**是在追一个不存在的东西**。

**判据本身没算错,它算的是「这一格平均有多差」,而我问的是「这一格有没有在变好」。这是两个问题。** 所以现在同时打印**首梭和末梭**:

> **一个在被优化的过程里取的平均值,看不见那个优化。** 要判断收敛,判据必须是**有序的**(首/末、斜率、尾窗),不能是聚合的。

同一条已经在别处咬过一次——收敛闸原来拿整格历史当证据,改成尾窗之后,同一格从「4.7% 没收敛」变成「0.7% 收敛了」。**那次和这次是同一个错误的两个实例,而我没认出来。**

⚠ **收敛闸本身 2026-08-08 退场了**(没有轮次就没有收敛),但这条法则**没有**——它在新坐标里当晚又咬了三次,每次那个数都是算对的:读了按大小排序的表头当分布、用端点比值当增益、只看末点就说 `y_true` 是倒 U。全部记在 `MODEL.md` §5之二。**判据换了,这条没换。**
