# 压枪曲线的模型

**主法则。任何地方与它冲突,以它为准。**

这份文件只写**模型**和**常数**。排程规矩在 `calibration/CLAUDE.md`,固件和抓帧的事实在各自的代码注释里。

> ⚠ **这里不写错误史。** 每条结论「怎么得出来的、错过几次、被什么推翻」全在 `docs/model_error_history.md`——那是这份文件 809 行时的快照,**故意不进 git,模型确认之后就删**。冲突时以这边为准。

---

## 一、模型

```
y_true(t) = y_obs(t) + C(t − M)          还原枪的真实后坐力
y_obs(t)  = Σ dy_px / K  + human         屏幕上视角走了多少
```

| | 是什么 | 谁定的 |
|---|---|---|
| `y_true(t)` | **要拟合的东西**:压枪幅度随时间。枪的踢腿 | 输出 |
| `C` | 固件实际播的时刻表,**从设备读回** | 设备 |
| `M` | 一个 count 变成光子的延迟 | 测的 |
| `K` | 屏幕像素 / 鼠标 count | 测的 |
| `human` | 同一区间里手动了多少 | 固件上报 |

**拟合:** 全库汇总 → **按整条轨迹**聚类 → 取最大簇 → 一次拟出 17 ms 网格的折线 → 上传。
**没有 α、没有 EMA、没有轮次**,所以「这一格收敛没有」这个问题不存在,只剩「样本够不够」。

---

## 二、三条前提

不是结论,也不是待验证的假设。**没有它们就没有可拟合的对象。**

### 2.1 `y_true(t)` 是固定的

不随场次变,不随时间变,不随第几次跑变。一条今天拟、明天播的曲线,前提就是它明天还是同一条。

允许的最弱化:**游戏在 N 条固定曲线里选一条**(还能做:拟 N 条再分类)。而 **N = 1 是量出来的**——拟合器的聚类,50 梭 mp5k bare:`best separation 1.26x (need 8.0) — one population`。

### 2.2 输入要先有理由

> 一个量能当输入,是因为它有**物理或设计上的理由**,不是因为加上它拟合变好了。

**墙钟时间、第几个场次、第几次跑——都不许当输入。** 游戏的后坐力曲线是设计出来的常量,它不知道现在几点。

⚠ `t` 合法,而且它就是坐标——但它是**距点击多久**,跟墙钟是两个量。
⚠ 上面那张表**就是全部输入,没有第六项**。帧率测过(77→158 fps,斜率 +1.4%,r=0.14,n=65),不显著,不进。

### 2.3 两次读数不一样 ⟹ 至少有一次错了

**不许写成「它漂了」。** 「漂移」只能是一个**还没定位的测量错误**的占位符,而占位符归第五节。

---

## 三、常数现值

| 常数 | 值 | 怎么测的 | 状态 |
|---|---|---|---|
| `K`(red_dot) | **1.5413** | 两个 run 的实弹梭等效值 1.5416 / 1.5250 夹住它 | 🟡 两次差 1.08% ⟹ 有一次错了。**找到那个错之前不动** |
| `M` | **20 ms** | 三个独立来源:18.3–21.7、24.3 [18.0,28.4]、**18.9**(真实工况) | ✅ |
| `D` | **−19 ms** | 每梭 RMS:−50→15.9、−36→13.5、**−19→6.8**、−5→7.7 | 🟡 跟 −5 分不出。**它不进拟合**(见下),所以是调参 |
| `human` 的符号 | `+` | 逐帧回归 `screen_px = −0.0464 × hand`,r = −0.825 | ✅ 约定相反,固件的 "SUBTRACT" 和代码的 `+` 同时成立 |
| `human` 的幅度 | — | `\|slope\|` 对 `K` **差 33 倍** | 🔴 对齐不了。**休眠中**(全库 131146 个间隔里 2 个非零) |
| `η`(交付率) | **不存在** | 三次 0.971/1.004/0.980,均值 0.9850 ± 0.0098,**离 1 是 1.52 σ** | ✅ 模型不需要这一项 |

⚠ **`C` 从设备读回,所以 `D` 不进拟合**——`D` 写错了只让屏幕多留一点残差。**唯一进拟合的是 `M`**:少写 `M` 会得到 `F_{n+1} = y_true + M·F_n′`,一个带导数的迭代,增益 `M·ω`,高于约 8 Hz 每轮放大,而 17 ms 网格到 59 Hz。

⚠ **`M`、`D` 存在每一梭上**,不是分析时读常量——上周那一梭走的是另一条显示链。

### 3.1 相关器的逐对偏差 `b(δ)`

每读一对帧,读数比真值多 `b(δ)`,而 **`b` 是那一对自己位移的函数,会变号**(两个独立 run):

```
δ px      0      0.6    1.1    2.0–2.2   3.8–4.4   7–8
b px/对  0.000  +0.047 +0.076   +0.039    −0.098   −0.21
        （静止）                （实弹梭中位数）
```

**δ ≈ 3 px 穿零**(亚像素峰锁 / 帧内运动模糊)。一梭的 δ 分布横跨零点,正负对消:全库 272 梭代入 → **−0.28% / −0.81%**。

⚠ **相关器按 `RECOIL_PATCH_H = 256 px` 整数混叠,而 `max_abs_frame` 看不见**——混叠的那一对报出来是个**小**数。

---

## 四、`y_obs` 怎么量、离群怎么剔

**锚住不动,别攒。** 相位相关只在有共同内容的两帧间成立,而窗口盖得住:

```
半个 patch 窗口（过半会 wrap）  128 px
一梭累计位移 |cum_px|  p50 98 px  p90 415 px
```

主路锚在第 0 帧直到逼近半窗;**逐帧链保留当交叉校验**——两条路算同一个量,不一致就是抓到了错。

**聚类的单位是一整梭,不是一个点。** 让一梭报废的东西(手动了、配件没装上、掉出 ADS、相关器跟丢)**污染整条轨迹**,而那条轨迹看起来完全合理:光滑、单调、量级正常。**逐点剔除永远抓不到它。**

⚠ 阈值从数据里出,不许硬编。⚠ 被丢掉的梭要记下来。

### 4.1 模型唯一的外部检验:**`y_true` 与当时播的是哪条曲线无关**

这是拟合器**安排不出来**的那一条。最强的一次通过(同一 run,逐梭轮换):

```
曲线 counts     n    y_true(2.40s)   视角位移
       0        5        853.8         853.8
     473        5        853.5         428.8
     945        5        850.4          10.1
                         位移跨 85 倍，一致到 0.4%
```

⚠ **必须插花**(让两条臂共用同一次测量的一切未知),**必须复现**(一次插花的 4.7 σ 被一次复现推翻过),**分臂按「交付了多少」而不是 `comp_enabled`**(`--scale-sweep=0` 的梭旗标也是 `True`)。

⚠ **采集打补偿臂,不是空臂。** 空臂 `y_true = y_obs`,答案 100% 来自测量;补偿臂 `|y_obs|` 只有 36 counts,**任何乘性的测量错都被除以 23**。这是一个零位测量:越接近答案越精确。

### 4.2 底噪:能分辨多少

| | |
|---|---|
| 逐梭散布 | CV **2–4%** —— **枪自己的**逐发随机,`sem = CV/√n` 有效 |
| 尺子占其中多少 | **≤0.5%** —— 所以把相关器做得更准一分钱买不到,唯一的杠杆是多打几梭 |
| 同一 run 内、插花、每臂 n=8 | sem 1.1% → **3% 的真效应看得见** |
| 跨 run | 补偿臂干净;**空臂额外 2% 的乘性罚款**(6.1 第 2 条) |

**排程怎么排(八格轮转、每格几梭)在 `calibration/CLAUDE.md`。**

---

## 五、跨配置:**不合并,存整条,永不拆**

> 每个配置存自己那一条完整曲线,**绝不拆成逐槽的乘数**。

理由不是「怕不准」,是**出问题时要分得清是哪一槽**。而且两条实测说拆开本来就不对:

- **因子是 `t` 的函数**:`comp_ar` 在 t=0.3 s 是 0.9227,t=3.2 s 是 0.7675,**跨 17%**
- **「槽位相乘」是每把枪自己的性质**:m416 上基本相乘(±4%),而 mp5k 的 muzzle×grip 差 **+13.8%** [+11.0,+16.4]、三件全装差 **+23.6%** [+17.9,+28.7]

八格全测的两张表在 `pixi run cube` 的输出里,mp5k 那八格**两条流水线各测过一遍,七格差 ≤3.7%**。

---

## 六、待做的,和已经排除的

> 一条要进正文,门槛两样:**测出来 + 换一次复现**。

**停止规则:当恒等式里每一项都被「读源码」以外的东西量过至少一次,而剩下的每个分歧都小于消不掉的底噪时,验证就完成了。**

### 6.1 还没完的

| | 什么 | 状态 |
|---|---|---|
| **1** | **`comp_counts_at` 跟固件对不上** | ⚠ **唯一还没完的模型问题,而且不是参数,是一个算错了的函数。** `curve/move` 随保持时长上升,两个 run **+0.0148/s(2.8 σ)** 和 **+0.0242/s(5.7 σ)**;而「每 count 推多少像素」不可能知道你按了多久,所以那条趋势只能是积分的错。峰在**曲线播完那一刻**且此后不变 → 不是释放冻结,是**末段**,那正是 `int(accum)` 带进位那段代码 |
| 2 | 那个 1–2% 的乘性测量错在哪 | **猎捕,不是刻画。** 候选:视角走到俯仰非线性段、抓帧丢帧、腰射尺子不平、patch 落在低纹理上。每个单独造出来,看能不能复现 |
| 3 | 形状上那 0.55% | **上界,不追**——5.2 counts / 942,比底噪小 4–8 倍 |
| 4 | `b(δ)` 在 δ = 8–31 px 的空档 | 界是 −0.36%…+0.17%,而**今天没有东西读 `b(δ)`**。用上了再补 |
| 5 | `D` 在 [−19,−5] 分不出 | **不是模型问题**:`D` 不进拟合 |
| 6 | `human` 的时间对齐 | 休眠中,**任何「边玩边观测」一上线就承重** |

### 6.2 已经排除的 —— 不要再推导一遍

| 假设 | 怎么死的 |
|---|---|
| **`η`:曲线只有 96–97% 到达屏幕** | **是噪声。** 三次读数散布 1.73%,而一次臂间差**自带** sem 1.50%。均值离 1 是 **1.52 σ** |
| `η` 的四个候选机制 | 全部单独证伪:**SIZE**(240 个 1-count 给满 K)、**PATH**(少发 0.90% 不是 4%)、**后坐力回复**(85 倍位移无趋势)、**记账错误**(6.1 第 1 条:模型算**少**了,方向反) |
| 曲线比真实后坐力大 4.3%,不动点让准星恰好正确 | 从 φ=0.957 推的,而三次记录没有一次给出那个值 |
| **`K` 上下不对称 6.32%** | 干净测出来 **0.19%**。那个 6.32% 来自 n=3、同一个 run、还过了混叠还原 |
| **相关器每对超读固定 ~0.04 px,一梭高 7.5%** | 那是 `b(δ)` 在 δ≈0.6 那一端的值,不是常数。一梭净值 −0.28% / −0.81% |
| **`eps ≈ 0`,命令多少就到多少** | 那是 ±10% 扫描的**零点**(回路会把它推到 1),不是**斜率** |
| **run 之间有 2.7% 的台阶** | 那九个 run 里臂和时间共变:前三个纯空臂(821.1)、中间四个纯补偿臂(865.4),差 5.1% |
| **impulse 对齐检查**(在第 k 发放个尖峰,看哪一发动) | ⚠ **不许再用,三条独立的理由:①时刻记不准 ②两套坐标之间本来就对应不准 ③弹药计数器在开火时读不出来**——`control/fire.py` 记着它「一梭 42 发里只读得出约五次」,**根本没有逐发分辨率**。而它在时间坐标下也没有指称对象了:只剩点击一个原点,还是本仓库自己发的。它留下的**形状**(一个拟合安排不出来的信号)由 4.1 的臂间一致接替 |

### 6.3 闸门还缺的

⚠ **主路没有复现闸。** 一次拟合吃全库,所以「复现」在那条路上要问的是**按次切开、每片各自拟,曲线一不一样**。`pixi run session-split` 做了一半。

⚠ **2.3 是法律,但没有东西执行它。** 两次读数对不上时,正确的行为是**拒绝出数**,而现在的工具只是并排报告。
