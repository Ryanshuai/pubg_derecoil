# 压枪曲线的模型

**这份文件是主法则。任何地方(代码、注释、其它 `CLAUDE.md`)与它冲突,以它为准。**

它和根目录 `CLAUDE.md` 的分工:那边是**教训**——踩过的坑、付过的账;这边是**规格**——要拟合的是什么、用什么坐标、数据长什么样。教训仍然全部有效,只是有几条是在旧坐标里写的,标注见文末。

---

## 一、模型

```
y_true(t) = y_obs(t) + C(t − M)             C(s) = F(s − D)
y_obs(t)  = Σ dy_px / K + human
```

| | 是什么 | 谁定的 |
|---|---|---|
| `F` | 要拟合的那条曲线,「压枪幅度随时间」 | 拟合的输出 |
| `D` | 固件相对**点击**什么时候开始播(`RECOIL_FIRE_DELAY_MS`) | **选**的:让漂移最小 |
| `C` | 固件实际排出去的时刻表,**从 Pico 读回**,`D` 已经烙在里面 | 设备 |
| `M` | 一个发出去的 count 多久变成光子(`RECOIL_COMP_LAG_MS`) | **测**的 |
| `K` | 屏幕像素 / 鼠标 count | **测**的 |

**要拟合的是一条「压枪幅度随时间」的曲线,而固件播的正是这个。** 中间那层「42 个弹桶」是分析这一层自己加的,枪和固件都不需要它。

⚠ **`y_true` 是时间的函数,不是弹号的函数。** 一发一发的踢腿是这条曲线的形状,不是它的坐标。

⚠ **`C` 必须从设备读回,不能重建。** 读回来的曲线里已经含着 `D`,所以**`D` 对不对根本不进拟合**——写错了只让屏幕上多留一点残差,不会污染样本。**唯一进拟合的是 `M`**,而 `M` 错多少,回路的正反馈增益就是多少(`ΔL·ω`,见下)。

### 一之二、⚠ 这条恒等式 2026-08-08 之前少两项,而少 `M` 那一项是**发散**的

**少 `M`(analysis 写 `C(t)` 而不是 `C(t−M)`)不只是偏 `M·F′`。** 拟合吃自己的输出,所以

```
F_{n+1} = y_true + M · F_n′
```

是一个**带导数**的迭代,在频率 ω 上增益 `M·ω`。高于 `1/M`(M=20 ms 时约 8 Hz)每轮放大,而 17 ms 的网格到 59 Hz。**跟根 `CLAUDE.md` 里那个 1.025²⁵⁵ 的移动平均是同一个形状。**

⚠ **`M` 和 `comp_lag_s` 存在**每一梭**上,不是分析时读常量。** 一个后来读的常量描述的是「机器现在什么样」,而上周打的那一梭走的是另一条显示链。`D` 同理(`Magazine.fire_delay_ms`)——**而 `D` 曾经被认为「不用记,`curve[0]['t_ms']` 就是它」,那个说法只在唯一一个正偏移的批次上验过**:`upload_pattern` 把负偏移的结折进 `t=0`,全库 50 梭因此一律读 0,分不出 −90 / −70 / −50 / −36 / −30 / −10。

### 一之三、⚠ `η` 撤回 —— 它是**一批数据上的过拟合**,不是模型里的一项

**2026-08-08 深夜先立后撤,全过程留在这里,因为撤回的理由比那个系数值钱。**

立的那一轮(run A,19:57,曲线 948 counts,同场次逐梭交替):

```
scale 0  n=8   840.0 ±5.2      scale 1  n=8   867.6 ±2.8      gap +3.28%   4.7 σ
拟合出 η = 0.9711 之后，两条臂整条路径重合到 rms 2.9 counts (0.66%)
```

当时我把它称为「这个模型最强的一句话」。**它是一批数据、一个系数、闭合到 0.66% ——那是过拟合的定义。**

撤的那一轮(run B,21:2x,曲线 945,**三档**、位移杠杆 **85 倍**、同样逐梭轮换):

```
curve      n   y_true(2.40s)   sem    位移 |y_obs|   vs scale-0
     0     5       853.8       5.7        853.8       +0.00%
   473     5       853.5       3.9        428.8       -0.04%
   945     5       850.4       6.2         10.1       -0.40%
```

**位移从 854 counts 扫到 10 counts,三条臂一致到 0.4%。没有 η。**

⚠ **而两轮之间,两条臂各自朝相反方向漂了约 2%**(scale-0 +1.64%、scale-1 −1.98%)。这个仓库记着的那个 ~3% 场次漂移**不是只加在一条臂上的**,它可以落成一个假的臂间差——**而插花只保证「同一场次」,不保证「同一场次内没有结构」。**

> **一次插花的 4.7 σ,被一次设计更强的复现推翻了。判据是:第二次实验的杠杆比第一次大 85 倍,而第一次那个效应在它下面消失了。**

**因此撤回的**:`η` 这一项、「曲线比真实后坐力大 4.3%」、以及「不动点让准星恰好正确」那整套推论。**恒等式不需要 η。**

**因此保留的**:run B 是这道「拟合器安排不出来」的闸**有史以来最强的一次通过**——三档补偿量、85 倍位移跨度、`y_true` 一致到 0.4%。

⚠ **两个曾被拿来解释 η 的机制,验的过程仍然有价值,结论是它们本来就不需要解释什么:**

| 候选 | 结果 |
|---|---|
| **SIZE**:游戏对「240 个 1-count 报告」和「1 个 240-count」处理不同 | 240 个 1-count 的 `mouse.move()` 给出 **1.5357 px/count**,对 K=1.5128。**一个 count 都不丢** |
| **PATH**:固件的曲线播放器少发 | 同样 240 counts、同样 1.0 s、**两条臂都按住扳机**(否则空手那一拳只加在一条臂上):**0.90%** [0.08%, 1.72%] |
| **相关器偏差随每帧位移变** | 被符号否掉,见下 |
| **后坐力回复(游戏把视角往回拉)** | 被 run B 否掉:回复要求偏差随位移成比例,而 85 倍的位移跨度上一点趋势都没有 |

⚠ **「打完弹匣会自动退出红点」是操作台给的,而它作废了一个探针的设计**:一次长按里「打空之后」那一段是**腰射**,K 差 3 倍。那个探针的干烧窗口读出命令速率的 43%,而 `0.5/1.51 = 0.33`——**它量的是换算错了的 K,不是交付不足。**

⚠ **`0.90%` 那条仍然可能是真的**(p≈0.03),但它是**固件那条路少发 0.9%**,不是 4%,而且现在没有任何需要它解释的东西。

⚠ **第三条那次否定挖出了一个独立的问题,而它现在是未决的:`K` 依赖于它是怎么被测的。**

```
drop dups（calibrate_k，K 一直以来的定义方式）  n=32  K = 1.5171 ± 0.0030  px/frame 4.6
keep dups（collect_timed，每一梭的测量方式）    n=14  K = 1.5520 ± 0.0021  px/frame 2.8
                                                      差 +2.30%    9.4 σ
```

**`drop_duplicates` 变的不只是帧数,还变了「哪两帧在做相关」**——丢掉一帧就是拿 k−1 对 k+1,位移翻倍。所以两条路上相关器吃的位移大小不同,**至少有一条是有偏的**,而这个仓库里没有任何东西知道这两个数同时存在。

⚠ **`K` 因此没有改成 1.5520。** 「哪一个是有偏的那个」是它自己的未决问题,不回答它就装上任何一个,等于挑一个顺手的数。

### 一之四、`eps ≈ 0` 那个结论是**错的**,写在这里因为它会再犯

2026-08-08 报过「`eps = −0.38%`,命令多少就到多少」。**那是 ±10% scale 扫描的零点**,而零点测的是「曲线在不在它自己的不动点上」——**回路会把它推到那儿,所以零点恒等于 1,它对交付率什么都没说。**

真正测交付率的是**斜率**,那次读出来 `−0.948` `[−1.10, −0.86]`,而我说了句「在 15% 以内,算 1」。**加上 scale 0 那条臂把杠杆从 ±10% 拉到 100% 之后是 `−0.9589` `[−0.9766, −0.9470]`,不含 −1。**

> **一个良态方向和一个共线方向印在同一张表上,读错哪个都不会报错。**

### 为什么可以跨轮累积

每一梭打的时候 `y_comp` 都不一样(曲线在被改写)。但只要**每个样本记下当时在播的是哪条曲线**,加回各自的 `y_comp` 之后,**不同曲线下打的梭估的都是同一个 `y_true`**。

所以:

- 样本**永久保留**,跨轮、跨曲线、跨天累积
- 每次重新拟合**全量**,不是在上一条曲线上迭代
- **没有 α、没有 EMA、没有收敛闸**

⚠ **「这一格有没有收敛」这个问题整个消失了。** 剩下的只有「样本够不够」——那是个可以直接数的量。根 `CLAUDE.md` 里关于收敛判据的三节(整格均值 / 首末梭 / 尾窗)全部只适用于旧的迭代式回路。

---

## 一之五、常数现值和它们各自的证据(2026-08-08 全部重测)

| 常数 | 值 | 证据 | 状态 |
|---|---|---|---|
| `K`(red_dot) | **1.5128** | R² 0.99984,CV 1.1%,n=32,双向,慢速注入 | ✅ 对旧值 1.5474 是 **11 σ** |
| `RECOIL_COMP_LAG_MS` (`M`) | **20** | 三个独立来源:18.3–21.7(冲击探针)、24.3 [18.0,28.4](五臂扫描)、**18.9(真实工况)** | ✅ |
| `RECOIL_FIRE_DELAY_MS` (`D`) | **−19** | 每梭 RMS:−50 是 15.9、−36 是 13.5、−19 是 **6.8**、−5 是 7.7 | 🟡 好过 −36 是 97%,**跟 −5 分不出**;取 −19 因为它同时是 `−M` |
| `η`(交付率) | **1.00** | run B:补偿量 0 / 473 / 945 counts,位移跨 85 倍,`y_true` 一致到 **0.4%** | ✅ **模型不需要这一项**;run A 的 0.9711 已撤回 |
| `+ human` 的符号 | — | 全库 131146 个区间里 **2 个非零** | ❌ **从没被行使过,符号从没被测过** |
| 帧率进不进模型 | 不进 | 77→158 fps,`y_true` 全程斜率 +1.4%,r=0.14,n=65 | ✅ 不显著 |

⚠ **`K` 从来没被干净地测过,而查出来的原因值得单独记:相关器按 `RECOIL_PATCH_H = 256 px` **整数混叠**。** 存档 98 行里 76 行的 patch 互相对不上(中位分歧 76%),而活下来的 22 行还从 0.39 跳到 1.59——**所有 patch 一致地读小**,因为它们一起混叠。两行同一个 run 给出机制:`382.77` 和 `126.76`,差 **256.01**。

> ⚠ **`max_abs_frame` 看不见它。** 混叠的那一对报出来是个**小**数,所以那个本该抓「帧间位移过大」的统计量,正是混叠藏身的地方。对得上的行和对不上的行,`max_abs_frame` 中位数是 91 和 92 px。

⚠ **由此推出的「1.5474 是上下两个方向的混合、该用 `K_down`」整条论证作废**:干净测出来上下不对称是 **0.19%**,不是 6.32%。**常数确实该往那个方向动,但理由是另一个——这是「对」得最差的一种。**

---

## 一之六、两条臂那道闸:**不插花就不是判据**

`y_true` 与当时播的是哪条曲线无关——这是拟合器安排不出来的那一条。但**跨场次做,它测的是两个下午**:

```
同一格，同一个问题
  跨场次（OFF 14:37–15:33，ON 17:29–19:24）   +6.41%   5.3 σ
  修正 K 之后                                  +4.07%   3.4 σ
  同场次插花（run A，两档，逐梭交替）          +3.28%   4.7 σ
  同场次插花（run B，三档，位移跨 85 倍）      -0.40%   不显著   ← 复现失败
```

`config.RECOIL_FIRE_DELAY_MS` 记着**同一把枪、同一条靶道、相隔二十分钟、三十个 counts 的跨场次漂移**——950 上就是 3.2%,整个分歧的尺寸。

**所以 6.41% 拆成:K 错 2.3 点、跨场次 0.8 点,以及 run A 里那 3.3 点——而 run B 说那 3.3 点也是场次内的结构,不是臂的差别。**

⚠ **插花只保证「同一场次」,不保证「同一场次内没有结构」。** 两轮之间两条臂朝**相反方向**各漂约 2%,足以在一轮里造出一个 4.7 σ 的假差。**下一道闸该是复现,不是又一个更精巧的单轮设计。**

⚠ **闸门现在自己报告两条臂在时间上重不重叠,不重叠就打印 `NOT A VERDICT`。** 它不猜哪个读数对,只拒绝把一个跨场次的数字当成臂的差别端出来。

⚠ **分臂要按「交付了多少」,不能按 `comp_enabled`。** 一条 `--scale-sweep=0` 的梭armed 着 174 个全零的结,`comp_enabled` 是 `True` 而 `y_true = y_obs` 精确——**那个旗标答的是「固件上没上膛」,不是「有没有量被交付」**,而它把专门为插花打的 8 梭放到了补偿那一侧。

---

## 二、它消掉的东西 —— 不是做得更准,是不再存在

| | 弹号坐标 | 时间坐标 |
|---|---|---|
| 开火频率 | `span/(n−1)`,进桶边,5% 误差累积成 2 发 | **不出现** |
| 弹桶归属 | 第 k 发的位移必须记进第 k 个桶 | **没有桶** |
| 两个原点 / `W = 13 ms` | 弹药计数器 vs 点击,差值只在 AUG 上量过一次 | **只有点击,而且是我们自己发的** |
| 游戏输入延迟 `L` | 未知,不敢碰 | **被曲线自己的形状吸收** |
| `np.interp` 的 `right=` 钳位 | vss 末发 ±100 counts 靠它翻硬币 | **不存在** |
| 收敛判据 | 整格均值瞎、首末梭、尾窗——三次改判据 | **不存在**,拟合是一次性的 |

⚠ **`L` 被吸收有一个前提:网格要能表示平移。** 见第四节。

---

## 三、表示:采样点 → 全留 → 聚类 → 最大簇 → 拟合

```
每帧一个样本  (t, y_obs)
      │
      ├─→ 落盘,永不删除,带上当时在播的曲线
      │
      ▼
  y_true 样本 = y_obs + y_comp        每梭一条轨迹
      │
      ▼
  聚类(单位是**整条轨迹**,不是单个点)
      │
      ▼
  最大簇 ──→ 拟合 ──→ 194 点 × 17 ms ──→ 上传
```

### 聚类的单位是一整梭,不是一个点

**总会有离群点,而它们几乎从不是散的。** 让一梭报废的东西——手动了、姿势不对、配件没装上、开火中途掉出 ADS、相关器跟丢——**污染的是整条轨迹**,而不是轨迹上零星几个点。

而且被污染的那条轨迹**看起来完全合理**:光滑、单调、量级正常。逐点的离群剔除**永远抓不到它**,只有把它和别的梭放在一起比才看得出来它整个偏在一边。

所以:

1. 每梭的 `y_true(t)` 重采样到公共网格 → 一梭变成一个向量
2. 在这些向量上聚类
3. **取最大簇**,只用它拟合

⚠ **阈值必须从数据里出来,不许硬编。** 这个仓库里每一个拍脑袋的阈值最后都成了一道只验过一侧的闸(`MARGIN_MIN` 拒 0%、`ADS_FRAC_MIN` 55 梭全是误拒)。聚类的距离标度要由簇间间隔本身给出。

⚠ **被丢掉的梭要记下来,连同它为什么落在别的簇里。** 一道说不出自己拒了什么的闸,改不动也验不了——根 `CLAUDE.md` 里那条「闸门会审查掉你用来重调它的数据」在这儿一字不改地成立。

---

## 三之二、`y_obs(t)` 怎么量:锚住不动,别攒

相位相关只能在**有共同内容**的两帧之间做。所以「和第 0 帧比」能不能成立,取决于**窗口盖不盖得住位移**。量出来是盖得住的:

```
red_dot 半个 patch 窗口(过半相关峰会 wrap)    128 px = 82.7 counts
一梭累计位移 |cum_px|      p50      98 px     ← 比窗口还小
                           p90     415 px
                           max    8682 px     (裸枪 / 坏梭)
```

**中位数那一梭整梭走完都没出一个窗口。** 压好的梭里第 0 帧就是全程有效的参考。

⚠ **而现在的实现是逐帧差分再 cumsum 约 300 次——它把一个 1 步的直接测量变成了 300 步的随机游走。** 不是精度不够,是白送出去的。

所以:

| | |
|---|---|
| **主路** | 锚在第 0 帧,一直用,直到位移逼近半个 patch |
| **换锚** | 只在逼近时换,**并记下换在哪**。p90 那类梭约 4 次,裸枪几十次 |
| **逐帧链** | **保留**,只当**交叉校验**——两条路算同一个量,不一致就是抓到了错 |

最后一条不是冗余癖:这个仓库的历史全是「自洽的错数」,而**两条独立路径给同一个量**是唯一能抓住它的东西。代价接近零——相关是开火结束后离线跑的(`MagazineRecorder` 先攒 patch 再做 FFT),不占帧预算。

⚠ **逐帧也不是永远安全**:`|dy|` 每梭最大值 p99 = **89.4 px**,已经是 128 px 窗口的 70%。`out_of_range` 那道闸两条路都要。

### 这也消掉了「帧率越高噪声越大」那个权衡

逐帧 cumsum 下,帧率 ×2 ⇒ 累加步数 ×2 ⇒ sd ×√2,**165 Hz 是亏的**。

锚住不动之后,**累加次数 = 总位移 / 窗口,和帧率无关**。于是提帧率只买时间分辨率,不付噪声。**窗口高度因此成了一个真的旋钮**:patch_h 从 256 加到 512,换锚次数减半,而抓取从 0.45 Mpx 涨到 0.9 Mpx——仍在 6.06 ms 的预算里。

⚠ **「每对噪声是恒定的还是正比于位移」这个问题现在答不了:逐帧数组从来没落过盘**,3073 梭的记录里只有 `mean_mad` 这类聚合量。这正是本模型要修的那件事,而它连自证的数据都没有。

---

## 四、固件那一侧(2026-08-08 读码确认)

| | |
|---|---|
| 播放锚点 | `fire_start_ms`,即**点击** |
| 分辨率 | **1 ms**(`get_recoil_delta` 每毫秒被调一次) |
| 节点间 | **线性摊开**——`spread_dy_per_ms = dy / dur`,`dur` = 到下一个节点的时间 |
| 容量 | **300** 个节点(`MAX_PATTERN_POINTS`,host `MAX_POINTS` 与之匹配) |
| 抖动 | ±2% 幅度 + ±0.2 count,**零均值**,只为打散完全可复现的模式 |

**所以固件本来就是一个时间域的折线播放器,正是这个模型要的东西。** 挡在中间的只有一处:

> `press/pico_mouse.py: upload_pattern()` —— docstring 第一句就是 “Upload pattern merged to **one point per bullet**”。它拿 `bullet_interval_s` 把任何细网格重新合并回一发一个点。

### 42 个节点的真实代价(不是「一坨」)

⚠ **曾经写成「42 个 85 ms 的大坨」,那是错的,已收回。** 累计曲线在节点之间是**线性**的,所以 42 节点 = 一条 42 段的折线。

代价在**段内**:AUG 一个 88 ms 间隔内的踢腿(补偿关掉,折叠到一个间隔上,12 个采样),原测于 `tools/probe_kick_profile.py`——⚠ **那个探针 2026-08-08 随弹桶坐标删了**(它 import `calibration.analysis`),**数本身还活着**:同一组值抄在 `press/pico_mouse.py` 的 `upload_pattern` 注释里,那是它现在的出处:

```
0.9  1.3  0.7  2.7  2.5  2.3  2.2  1.9  1.6  1.2  1.2  1.4
```

**间隔内速率变化 4 倍**,而 85 ms 一段的折线在段内**速率恒定**。拿恒定速率去追这条剖面,准星在每个间隔内来回漂 **±10% 一发的量**,而 42 节点的曲线**一点都去不掉**——它没有那个自由度。

17 ms 的网格把这个剖面切成 5 段,能追。

---

## 五、采样:165 Hz 是硬顶,现在拿到 53%

Desktop Duplication 给的是合成器**已经合成**的帧,所以上限就是刷新率(实测 **165 Hz / 6.06 ms**)。

拿不满的原因是每帧抓了逐帧用不上的东西,而 **DXGI 只能有一个 bounding box**:

| 每帧抓什么 | 区域 | bbox | 耗时 | 占 6.06 ms |
|---|---|---|---|---|
| 现在(patch + 弹药 + 火力模式 + 枪名 + 配件槽) | 23 | 1911×1244 = 2.38 Mpx | 5.41 ms | **91%** |
| **只要 tracker 的 patch**(red_dot) | 7 | 1768×256 = 0.45 Mpx | **1.62 ms** | **27%** |
| **只要 tracker 的 patch**(vss_pso1) | 3 | 873×256 = 0.22 Mpx | **0.95 ms** | **16%** |

**时间坐标下每帧只需要 patch。** 弹药、火力模式、姿势、枪名一个都不进逐帧路径——它们回答的是「这一梭算不算数」,那是每梭一次的问题。

165 Hz 对 17 ms 的固件步长是每步 2.8 个样本。**而相位是白送的**:点击落在 vsync 哪个相位每梭都不同,10 梭叠起来样本自己在 6 ms 的格子里抖开,亚帧分辨率不用争取。

---

## 五之二、⚠ 2026-08-08 夜:第一批真数据,以及它打破的东西

18 梭 m416 裸枪、红点、站立,同一把枪同一次持握,只有补偿开关不同。**这是本模型第一次被真实数据检验,而它在一处被打破了。**

模型说:开着补偿和关着补偿打的梭,各自加回自己的 `y_comp` 之后,`y_true` 应该相等。

```
comp OFF   n=9   y_true = 774.3 ± 27.8   (3.6%)      ← 直接测量,y_true = y_obs
comp ON    n=9   y_true = 901.9 ± 98.5  (10.9%)      ← +127.7 counts = +16.5%
                                          Welch p = 0.0014
```

**不相等,而且差得有结构。** 逐时刻的隐含增益(生效的补偿 ÷ 命令的补偿):

```
t=0.15s   g = 0.655        ← 第一发的时序
t=0.55s   g = 0.961
t=1.75s   g = 0.986
t=2.15s   g = 0.975        ← 中段:游戏就是 1:1,模型在这里是对的
t=2.55s   g = 0.971
t=2.95s   g = 0.944
t=3.75s   g = 0.915        ← 尾部:瞬时增益只有 0.78
```

### 中段 0.96–0.99,所以「游戏有个 0.92 的增益」是假的

那个 0.92 是被两头拖出来的平均值。**又一次「聚合量看不见它要管的维度」**——如果只报端点比值,得到的就是一个不存在的常数增益,而且它会被当成一个可以乘进去的修正。

### 尾部:补偿越过头,生效比例越低

最后三分之一里补偿命令了 +444 counts,屏幕上净生效 **348**,瞬时增益 **0.78**。而那时 `y_obs` 已经到 **−592 counts**——视角被按到原瞄点下方 592。

**当时提出的机制是游戏自己的回正**:把准星压到原瞄点以下,游戏往回顶。它预测 `y_true` 随补偿强度**单调**变化。

### ❌ 而扫一遍曲线强度就把它证伪了 —— 是个倒 U,不是单调

`collect_timed --scale` 按倍数缩放上传的曲线,28 梭:

```
曲线          n    y_true               y_obs 末    comp 生效
no-comp       9    774.3 ± 27.8          +774.3         0.0
scale=0.5     5    892.0 ± 37.2          +156.9       735.1
scale=1       9    901.9 ± 98.5          −592.6      1494.5
scale=1.5     5    835.2 ± 20.2         −1420.7      2255.9
```

`y_obs` 从 **+774 扫到 −1421**,而 `y_true` 走的是 **774 → 892 → 902 → 835**。

**两端都低,中间高。回正预测的是单调,所以那个机制不成立。**

### ❌❌ 而「倒 U」本身也是假的 —— 那是只看末点的产物

上面那张表报的是 `y_true` 在 **t=3.8 s** 一个点上的值。按时间拆开之后,四档的关系每一段都不一样:

```
    t     no-comp    x0.5      x1.0      x1.5     极差
   0.3      35.3      38.0      42.3      44.8   23.9%   单调递增
   0.6     105.5     108.8     112.0     112.5    6.4%   单调
   1.2     252.7     257.8     262.5     262.9    3.9%   单调
   1.5     328.2     330.3     327.9     327.2    0.9%   ← 四档几乎重合
   1.8     411.0     418.1     423.0     426.6    3.7%   单调
   2.4     562.6     581.8     587.5     590.6    4.8%   单调
   2.7     631.9     662.7     672.0     649.9    6.1%   ← 只有 x1.5 翻头
   3.3     763.8     822.8     856.0     792.2   11.4%
   3.8     774.3     892.0     901.9     835.2   15.0%
```

**是三件事,不是一件:**

| 段 | 现象 | 幅度 |
|---|---|---|
| t ≈ 0.3 s | 随曲线强度**单调**递增 | 23.9%,到 1.5 s 衰减掉 |
| **t = 1.5 s** | **四档几乎重合** | **0.9%** |
| t ≤ 2.4 s | 单调,不是 U | 3.7–5.6% |
| t ≥ 2.7 s | **只有 x1.5 那一臂**塌下去,其余三档仍单调 | 到 15% |

**所谓倒 U,全部是 x1.5 那一臂在最后 1.1 秒里掉下来造成的。**

### 于是结论是

> **模型的合并假设在中段站得住(t ≤ 2.4 s,极差 < 6%,t=1.5 s 上 < 1%)。** 四条强度差 3 倍的曲线在那里给出同一个 `y_true`,这正是第三节要的东西。

而要单独解释的是**两头**:开火起始约 0.3 s 的时序,和**过量补偿在晚期的塌陷**。后者有位置(t≈2.5–2.7 s)、有条件(只在 x1.5)、可复现——**是个能查的问题,不是一个形状**。

### 三条推论

1. **不能指望一次拟合到位。** 要迭代:拟合 → 上传 → 重采 → 再拟合。第二轮起 `y_obs` 就在平台区里。
2. ⚠ **「关掉补偿那一臂是无偏的锚」是错的。** 它的 `y_true` 在 t=3.8 s 上是全场最低,比平台低 13%。**当时我把它当成最可信的直接测量,那是在拿偏得最狠的一点当基准。**
3. **不再猜成因。** 这个仓库前一晚因为连着猜撞了六次同一个形状。

### ⚠ 而这一节自己就是那条法则的第三次现场

同一个晚上,三次都是**把一个聚合量当成了它要管的那个维度**:

| 报出来的 | 真相 | 差在哪 |
|---|---|---|
| 「220 格 sys/e = 0.93–0.99」 | 中位 **0.78**,166/220 低于 0.90 | 读了**按大小排序的表头** |
| 「游戏有 0.92 的增益」 | 中段其实 **0.98** | 用了**端点比值** |
| 「y_true 是倒 U」 | t ≤ 2.4 s 单调,t=1.5 s 上四档差 **0.9%** | 只看了**末点** |

根 `CLAUDE.md` 的「判据必须能看见它要管的那个维度」,三次都是它。**而每一次那个数都是算对的。**

⚠ **`scale=1` 那组 sd 98.5,是另外三组的 3–5 倍,而它混了两批**(第一批跑在 83 fps,后一批 130 fps)。**这个混淆没拆开**,所以那组的离散说明不了任何事。

### 顺带确认和排除的

| | |
|---|---|
| ✅ **打空的时刻可预测** | OFF 臂在 t=3.35 s 之后不动了(773.5 → 774.8)。40 发 × 85.38 ms = **3.33 s**,分毫不差 |
| ✅ **相关器一次都没丢** | 18 梭 **0 个 out-of-range**,`|dy|` 每帧 p99 只有 8–21 px 对 128 px 的窗口 |
| ❌ **我自己的一个 bug** | `comp_counts_at` 原来把曲线积分到底,而**固件在松开扳机时就停**(`get_recoil_delta` 的 `if (!firing) return`)。修掉解释了 154 counts 里的 **26.5**。`Magazine.hold_s` 现在记录松手时刻 |
| ⚠ **补偿开着时逐帧位移更大** | `|dy_px|` p99:ON **13–22 px** 对 OFF **8–10 px**。净位移更小而瞬时更大——补偿是按毫秒注入的阶梯,后坐力是连续的。ON 臂 10.9% 的离散大概来自这里,**没验过** |

### 而这些都是旧路径看不见的

同一批梭,旧的分桶路径对它们的报告是 **「residual −20.3 ± 31.1」** —— 一切正常。

---

## 六、这一节里哪些是量出来的

**量出来的**(2026-08-08,全部可复查):

- 固件的播放语义(第四节整表)、`MAX_PATTERN_POINTS = 300`
- `upload_pattern` 合并到一发一点
- AUG 的弹间隔内踢腿剖面(`pico_mouse.py` 的注释里存着)
- 165 Hz / 6.06 ms;第五节的抓取耗时表
- 循环实测 **88 fps 中位**(3131 梭的记录)
- **220 个格子的 `sum|sys| / sum|e|`:p10 0.54 / p50 0.78 / p90 0.96** —— 逐发误差里逐梭可复现的那一份。大头是偏差不是噪声,但中位仍有 22% 不可复现
- ~~桶边是 `np.arange(nb+1) * interval`,原点是弹药数字第一次变化~~ —— ⚠ **出处 `calibration/analysis.py` 2026-08-08 删除**。这一条是对**被这份文件取代的那个实现**的描述,它随实现一起退场;留在这里是因为第二节那张表引用它。**没有任何东西能再复核这两个行号了**,这是删掉实现时清醒付掉的代价。
- 逐发的出膛时刻**每帧都在算**,只留首尾和一个计数([fire.py:398-405](control/fire.py#L398-L405))
- 帧时间戳盖在**阻塞等待之前**([fire.py:392-396](control/fire.py#L392-L396)),标的是它自己都还不存在的那一帧
- `AcquireNextFrame` 的 `LastPresentTime` 到了进程里、三行后被丢掉(bettercam `core/duplicator.py:22`)
- `scar_att` 步长 100 ms 对实测火循环 92.26(**后半梭差 3.19 发**)、`ump45_att` 92 对 89.05(**1.19 发**)

**没量的,是一句预测:**

> 粗网格**是**那份可复现偏差的成因。

**这条一个字的实测都没有。** 在新模型跑出第一格、`sum|sys|` 真的掉下来之前,它是待验假设。这个项目 2026-08-07 一晚上因为把假设当结论撞了六次同一个形状,而这一条现在**长得跟结论一模一样**。

⚠ **两条我自己在同一天里写错又收回的,留着当样本:**

1. 「220 格 sys/e = 0.93–0.99」—— 那是**把按 `sum|sys|` 排序的前 14 行当成了分布**。全量中位是 0.78,166/220 低于 0.90。**读了有序表的头部就当成总体**,同一天第二次。
2. 「42 个 85 ms 的大坨」—— 固件在节点间是线性摊开的,它是折线不是台阶。**在读固件之前就断言了固件的行为。**

---

## 七、旧坐标下的哪些法则仍然有效

**只要 `analyse()` 还在按 `arange(nb+1)*interval` 分桶,下面这些就还生效**,它们跟着实现一起退场,不是现在:

| 法则 | 在哪 | 时间坐标下 |
|---|---|---|
| 对角约束(第 k 发的偏差修第 k 发) | 根 `CLAUDE.md` | **不变**,只是「位置」从弹号换成时间 |
| 观测对齐必须验(尖峰打在第 k 发,看哪发动) | `detector/CLAUDE.md` 第四铁律 | **退场**——没有桶就没有错桶 |
| 两个原点 / `W = 13 ms` | 同上 | **退场**——只剩点击一个原点 |
| 收敛判据要有序(首末梭 / 尾窗) | 根 `CLAUDE.md` | **退场**——拟合是一次性的,没有轮次 |
| 不许平滑、不许跨项 | 根 `CLAUDE.md` | **不变**,而且更强:聚类的单位是整梭,不是把邻居摊开 |
| 闸门要两侧都验、要记下自己拒了什么 | 各处 | **不变**,直接适用于聚类 |

---

## 八、跨配置怎么合并 —— **不合并。存整条,永不拆**(2026-08-08 实测)

配件因子那套东西在时间坐标下的对应物,这一节以前是空的。

### 规矩

> **每个配置存自己那一条完整曲线。绝不拆成逐槽的乘数。**

理由不是「怕不准」——下面的实测说它相当准。理由是**出问题时你要分得清是哪一槽**:一条按 `(weapon, config_key, posture, sight)` 归档的曲线,是那把枪那套件**直接测出来的**;一个由三个因子相乘得到的数,错了之后没有任何东西能说出错在哪一项。要求原话:「保存差乘结果的那个,不要拆那个数字,不然你有问题你不知道加满」。

`samples.py` 本来就是这么做的(按 `config_key` 归档),这一节把它从「实现碰巧如此」变成「规矩」。

### m416 的 2×2×2,八格全测(comp OFF,红点,站姿,梭末 t=3.415 s)

```
cell    n    counts   f vs bare        M=comp_ar  G=vert_grip  K=tactical_stock
···     5   1459.5    1.0000
··K     5   1328.0    0.9099
M··     5   1135.6    0.7781
·G·     5   1119.9    0.7673
M·K     5   1041.0    0.7133
·GK     5   1041.9    0.7139
MG·     6    893.1    0.6119
MGK     5    810.6    0.5554
```

配对超出(实测 ÷ 单件相乘),20000 次 bootstrap 95% CI:

```
M+G     +2.5%   [ +0.4%,  +4.6%]      区间勉强不含 0
M+K     +0.8%   [ −2.3%,  +3.9%]      含 0
G+K     +2.2%   [ −1.5%,  +6.3%]      含 0
M+G+K   +2.3%   [ −2.4%,  +6.9%]      含 0
```

**没有三阶项**:拿实测的 MG 乘 K 去预测三件,差 **−0.2%**。

**所以 m416 上配件因子基本是相乘的,精度 ±4%。** 而 M+G 那条的下界只有 +0.4%,还是四次未做多重比较校正的检验——**当作「悬着」,不当作已确证**。

### ⚠ 但「一条曲线 × 一个数」是**假的**

因子随时间漂,不是常数:

```
t=0.3 s   comp_ar 0.9227      t=3.2 s   0.7675      漂 17%
```

配件对**后段**的帮助远大于对头几发。按各自末点归一后形状差 1.7–3.4%(约 20 counts),所以「同一条曲线」作为**近似**站得住,而**「乘一个数」在时间上是错的,误差跟耦合同量级**。

这正是「存整条」的第二个理由:一条实测曲线自带这个时间结构,一个标量丢掉它。

### ⚠ 而我在这一节上把 §5之二 那条法则又犯了一次

**同一个耦合,三种算法给三个数,而每一次「弄干净」它都往零走:**

| 怎么算 | M+G 超出 |
|---|---|
| 录制末点、混合样本池 | +10.6% |
| **梭末点**、混合池 | +3.9% |
| 梭末点、只 comp-OFF、单会话 | **+2.5%** |

两个具体错误:① 末点在 t=3.81 s 而开火 3.415 s 就停了,后面 0.4 s **各配置漂的方向还不一样**(bare −18、MG **+12**);② 把 comp-ON 和 comp-OFF 混在一个池里平均。

**一路往零走是伪影的形状,不是效应的形状。** §5之二 说的是「聚合量看不见它要管的那个维度」,这里是它的第四个实例。

### mp5k 的 2×2×2,八格全测,而且**两条流水线各测了一遍**(`pixi run cube`)

这一格上一版写的是「⚠ 旧那张表里 mp5k 的非乘积**不能当作已复现**」。**现在复现了,而且是最强的那种复现**:`data/kit_factors.json` 是 08-05 用**已退场的弹桶坐标**、另一套代码、另一天测的同样八格,和今天时间坐标下的八格逐格对得上。

```
cell    n    counts     sd     cv  f(时间坐标)  f(08-05 弹桶)   差
bare   11     903.4   32.8   3.6%      1.000       1.0000     +0.0%
M..     5     518.7    3.8   0.7%      0.574       0.5907     −2.8%
.G.     5     669.5    8.2   1.2%      0.741       0.7470     −0.8%
..K     4     751.5    9.0   1.2%      0.832       0.8170     +1.8%
MG.     5     437.3    2.4   0.5%      0.484       0.4792     +1.0%
M.K     5     463.4    3.4   0.7%      0.513       0.4767     +7.6%   ← 唯一一格不合
.GK     5     538.5    7.5   1.4%      0.596       0.5907     +0.9%
MGK     5     395.1    2.1   0.5%      0.437       0.4216     +3.7%
                        M=comp_smg  G=vert_grip  K=heavy_stock
```

**八格里七格差 ≤3.7%。** 两条流水线要在同一个方向上同时错才能伪造出这个。

### mp5k **不是**相乘的,而 m416 是 —— 「槽位相乘」是**每把枪自己的性质**

```
配对            实测      单件相乘   超出      95% CI(20000 次 bootstrap,单位=梭)
muzzle × grip  0.4841    0.4255    +13.8%   [+11.0%, +16.4%]
muzzle × stock 0.5130    0.4776     +7.4%   [ +4.7%, +10.0%]
grip   × stock 0.5961    0.6164     −3.3%   [ −6.0%,  −0.7%]
三件全装        0.4374    0.3539    +23.6%   [+17.9%, +28.7%]
```

对比同一天同一套算法测的 m416:四条全部 ±2.5% 以内、三条含 0。

**所以不能说「配件相乘」也不能说「配件不相乘」。** m416 上相乘(±4%),mp5k 上 muzzle×grip 差 14%、三件差 24%。`control/kitting.py` 从 08-05 起就写着同一句话——「**『槽位不相乘』是 muzzle-grip 这条边的性质,不是模型的性质**」——今天在新坐标下拿到了同一个结论。

⚠ **`grip × stock` 的 −3.3% 别当已确证**:CI 上界只到 −0.7%,而且这是四次未做多重比较校正的检验里的一次。和 m416 的 M+G(+0.4% 下界)是同一个成色。**另外三条不是**——+13.8% / +7.4% / +23.6% 的下界分别是 +11.0 / +4.7 / +17.9,没有商量余地。

⚠ **`M.K` 那 +7.6% 是两条流水线唯一对不上的一格,原因不明。** 它同时也是 08-05 那边唯一被写成「+1.4 sigma,相乘」的配对。两条线各自内部自洽(cv 0.7%),所以这不是噪声。**记在这里,没有解释。**

### ⚠ 而这个立方体第一次测出来的时候,四个不同标签的格全是 0.48

```
grip=vert_grip                 435.4     ← 标签说只有握把
stock=heavy_stock              434.2     ← 标签说只有枪托
bare(那 1 梭)                  428.1     ← 标签说裸枪
comp_smg + vert_grip           437.3     ← 这个是真的
```

**四个格,一个数。** 而正确答案分别是 669 / 751 / 903 / 437。

三个错的全是同一个形状:**读的是一把枪,打的是另一把**。链条是——上一轮跑完把枪留在架上 → 这一轮 `ensure_weapon_in_hand` 找到它、`hold()` 失败 → **落进刷新分支又刷了一把** → `ensure_kit(1)` 装配并读 1 号枪,而手里是 2 号。`ensure_kit` 只在某一步是**装上**时才 `hold()`,所以一格只做拆卸的话它根本不换枪。

事后读架子是铁证:

```
gun1  红点，无枪口，vert_grip，无枪托      ← --kit 装的、read_config 读的
gun2  红点，comp_smg，vert_grip，战术枪托  ← 实际在手里、实际打的 5 梭
```

**这些格的 cv 是 0.5–1.4%。** 五梭彼此完全一致,曲线形状和其它格归一后差 1–3%,没有任何一个数看起来不对。唯一的破绽是**这一格复现了另一格的数**——而那需要有第二个独立来源才看得见。

⚠ **聚类没有兜住,而且这次它选错了一半。** 污染的 5 梭和干净的 5 梭在同一个文件里时,`separation 20.3×` 确实切开了,但「取最大簇」在 **5 对 5** 时没有意义——它留下的是**污染的那半**(拟合出 435.2)。这条已经进 backlog。

⚠ **而「heavy_stock 在 mp5k 上读不出来」是我编的,已证伪。** 当时的推理是「这一梭打了 435,对 bare 的 901 是 0.48,所以枪托装上了只是读不出」——**0.48 不是枪托的 0.83**,那是 comp+grip 的 0.479。这个格干干净净地读回 `{'stock': 'heavy_stock'}`,打出 751.5,对预测 738 差 1.8%。**一个从单格总数推出来的机制诊断,是 §5之二 那条法则的又一个实例**——那个 435 本身量得完全正确。
