# 压枪曲线的模型

**这份文件是主法则。任何地方(代码、注释、其它 `CLAUDE.md`)与它冲突,以它为准。**

它和根目录 `CLAUDE.md` 的分工:那边是**教训**——踩过的坑、付过的账;这边是**规格**——要拟合的是什么、用什么坐标、数据长什么样。

> ⚠ **正文只留验过的东西。** 没验的、以及验过并且是假的,全部在**第十三节**。
> 一条要从第十三节进正文,门槛是两样:**测出来** + **换一个 run 复现**(理由见第三、第四节)。
>
> 2026-08-08 这份文件被清过一次:当时正文里塞满了「这个曾经是错的,因为……」的叙事,而那是根 `CLAUDE.md` 的活。**一个混着规格和错误史的规格,读的人分不出哪句现在成立。**

---

## 零、这份文件的前提:**没有漂移**

> ### `y_true(t)` 是一条**固定的**曲线。它不随场次变,不随时间变,不随第几次跑变。
>
> **这是前提,不是结论,也不是待验证的假设。** 操作员的原话:「**这个没有漂移,永远没有漂移。游戏也不会这么设计。然后真实你也不能这么建模。**」

**为什么它必须是前提。** 允许「跟场次有关」当解释,整件事就塌了——一条今天拟出来、明天播的曲线,前提就是那条曲线明天还是同一条。**如果它会漂,那就没有可拟合的对象,这个仓库不该存在。**

允许的最弱化只有一条,而且它仍然是可建模的:

```
游戏在 N 条固定曲线里选一条          ← 还能做：拟 N 条，再分类
某个量每场次自己漂一点              ← 不允许。这不是模型，是给无知贴的标签
```

⚠ **而 N = 1 是量出来的,不是假设的。** 拟合器每次都在跑这个聚类,50 梭 mp5k bare:

```
no cut: best separation only 1.26x (need 8.0) — one population, all 50 kept
```

### 零之一、而「没有漂移」只是它的特例。一般的那条是:**输入要先有理由**

> **一个量能当模型的输入,是因为它有物理或设计上的理由,不是因为加上它拟合变好了。**

**墙钟时间、第几个场次、第几次跑、玩了多久——全都不许当输入。** 游戏的后坐力曲线是设计出来的常量:**它不知道现在几点。** 所以任何形如「随时间/随场次变」的项,不管它把残差压得多小,都是**把噪声拟合进模型**,而且拟出来的那个系数只在那一批数据上存在。

操作员的原话:「**一些明显不符合常识的东西,你就不能强行往上安。那个时间,绝对不是这个建模的一个输入。**」

**判据,加任何一项之前先答:**

```
这一项凭什么影响 y_true？          ← 用一句物理/设计上的话说出来
"它让残差变小了"                   ← 不是理由。那是加进去之后才该做的检查
```

⚠ **`t` 本身是合法的,而且它正是这个模型的坐标**——但它是**距点击多久**,是枪自己的踢腿剖面和固件自己的时刻表。**那跟「墙钟」是两个东西**,别因为都叫「时间」就混起来。

⚠ **仓库里最后一处拿「顺序」当输入的地方是 `fit_time_curve.RECENT_MAGS`**(只拟最近 50 梭)。它的注释自己写着「**IT IS NOT ABOUT STALENESS. y_true does not drift**」,真正的理由是 `|y_obs|`——在很错的曲线下打的梭给的估计有已知方向的偏。**所以那个窗口是一个代理,而正确的做法是直接按 `|y_obs|` 加权。** 在换掉之前,它是这条规矩唯一的例外,而且是**知情的**例外。

### 由此,一条**推理规则**,这份文件里到处在用

> **同一个量测两次不一样 ⟹ 至少有一次测错了。** 不许写成「它漂了」。

「漂移」在这份文件里因此**不是一个可以出现的解释**。它只可能是一个**还没定位的测量错误**的占位符,而占位符归第十三节,不归正文。

⚠ **这条规矩是 2026-08-08 深夜补的,因为正文当时有七处违反它**——`K` 每场次漂 1–2%、run 之间 2.72% 的台阶、形状上 0.55% 的「场次效应」……**每一处都是我把一个没查出根因的分歧,写成了系统的一个性质。**

而那一晚同时抓出四个真的测量错,每一个都能造出百分之一二的假分歧:视角顶到俯仰上限(那儿是天空,**瞎读出来是「没动」**)、抓帧循环卡住八分钟、腰射尺子不平而不同臂走不同距离、以及**臂和时间在旧数据里共变**。

---

## 一、模型

```
y_true(t) = y_obs(t) + C(t − M)             C(s) = F(s − D)
y_obs(t)  = Σ dy_px / K + human
```

| | 是什么 | 谁定的 |
|---|---|---|
| `F` | 要拟合的那条曲线,「压枪幅度随时间」 | 拟合的输出 |
| `D` | 固件相对**点击**什么时候开始播(`RECOIL_FIRE_DELAY_MS`) | **选**的:让逐梭残差最小 |
| `C` | 固件实际排出去的时刻表,**从 Pico 读回**,`D` 已经烙在里面 | 设备 |
| `M` | 一个发出去的 count 多久变成光子(`RECOIL_COMP_LAG_MS`) | **测**的 |
| `K` | 屏幕像素 / 鼠标 count | **测**的 |

**要拟合的是一条「压枪幅度随时间」的曲线,而固件播的正是这个。** 中间那层「42 个弹桶」是分析这一层自己加的,枪和固件都不需要它。

⚠ **这张表就是全部的输入,没有第六项**,而每一项都能用一句话说出它凭什么在这儿(零之一):`F` 是枪的踢腿、`C` 是固件排的班、`M` 是一个 count 变成光子要多久、`K` 是像素和 count 的换算、`human` 是手。**「拟合变好了」不能把第六项送进来。**

已经按这条判过的两项:**帧率**——测了(77→158 fps,斜率 +1.4%,r=0.14,n=65),**不显著,不进**;**墙钟时间 / 第几个场次**——**连测都不该测**,它们没有那句理由(零之一)。

⚠ **`y_true` 是时间的函数,不是弹号的函数。** 一发一发的踢腿是这条曲线的形状,不是它的坐标。

⚠ **`C` 必须从设备读回,不能重建。** 读回来的曲线里已经含着 `D`,所以**`D` 对不对根本不进拟合**——写错了只让屏幕上多留一点残差,不会污染样本。**唯一进拟合的是 `M`**,而 `M` 错多少,回路的正反馈增益就是多少(`ΔL·ω`,见下)。

### 一之一、`C` 从设备读回,所以只有 `M` 的误差会被放大

`C` 是 `read_pattern()` 返回的东西,`D` 已经烙在里面。**所以 `D` 写错了不进拟合**——只让屏幕上多留一点残差。

**`M` 不一样。** 拟合吃自己的输出,少写 `M` 会得到

```
F_{n+1} = y_true + M · F_n′
```

一个**带导数**的迭代,在频率 ω 上增益 `M·ω`。高于 `1/M`(M=20 ms 时约 8 Hz)每轮放大,而 17 ms 的网格到 59 Hz。

⚠ **`M`、`D` 都存在每一梭上**(`comp_lag_s` / `fire_delay_ms`),不是分析时读常量。一个后来读的常量描述的是「机器现在什么样」,而上周打的那一梭走的是另一条显示链。

⚠ **`D` 不能从曲线反推。** `upload_pattern` 把负偏移的结折进 `t=0`,所以 `curve[0]['t_ms']` 对**所有**负偏移都读 0。

### 为什么可以跨轮累积

每一梭打的时候 `y_comp` 都不一样(曲线在被改写)。但只要**每个样本记下当时在播的是哪条曲线**,加回各自的 `y_comp` 之后,**不同曲线下打的梭估的都是同一个 `y_true`**。

所以:

- 样本**永久保留**,跨轮、跨曲线、跨天累积
- 每次重新拟合**全量**,不是在上一条曲线上迭代
- **没有 α、没有 EMA、没有收敛闸**

⚠ **「这一格有没有收敛」这个问题整个消失了。** 剩下的只有「样本够不够」——那是个可以直接数的量。根 `CLAUDE.md` 里关于收敛判据的三节(整格均值 / 首末梭 / 尾窗)全部只适用于旧的迭代式回路。

---

## 二、常数现值和它们各自的证据(2026-08-08 全部重测)

| 常数 | 值 | 证据 | 状态 |
|---|---|---|---|
| `K`(red_dot) | **1.5413** | 两个 run 的实弹梭等效值 **1.5416 / 1.5250** 把它夹在中间(见二之一) | 🟡 **不动它**。两次读数差 1.08%,**所以至少有一次测错了**(第零节)——找到那个错之前,换成任何一个都没有依据 |
| `RECOIL_COMP_LAG_MS` (`M`) | **20** | 三个独立来源:18.3–21.7(冲击探针)、24.3 [18.0,28.4](五臂扫描)、**18.9(真实工况)** | ✅ |
| `RECOIL_FIRE_DELAY_MS` (`D`) | **−19** | 每梭 RMS:−50 是 15.9、−36 是 13.5、−19 是 **6.8**、−5 是 7.7 | 🟡 好过 −36 是 97%,**跟 −5 分不出**;取 −19 因为它同时是 `−M` |
| `η`(交付率) | **未定** | 三次:0.971(4.7 σ)/ **1.004**(不显著,0/473/945,位移跨 85 倍)/ **0.980**(6.2 σ,`K` 被 grid 独立钉到 ±0.02%) | 🔴 **两次显著、一次没有。** 而它跟「`y_comp` 积分算多 2%」在现有测量里**不可区分**——先做第十三节第 6 条。见四之二之二 |
| `+ human` 的**符号** | `+` | 人手动 12 s,逐帧回归 `screen_px = −0.0464 × hand`,r = **−0.825** | ✅ 测出来了,固件那句 "SUBTRACT" 和代码的 `+` 同时成立(约定相反) |
| `+ human` 的**幅度** | — | `\|slope\|` 0.0464 对 K 1.5413,**差 33 倍** | 🔴 **对齐不了**:手的计数器是快照,画面的 dy 是一整个帧间隔。逐帧相减只去掉约 3% 的手 |
| 帧率进不进模型 | 不进 | 77→158 fps,`y_true` 全程斜率 +1.4%,r=0.14,n=65 | ✅ 不显著 |

⚠ **`K` 从来没被干净地测过,而查出来的原因值得单独记:相关器按 `RECOIL_PATCH_H = 256 px` **整数混叠**。** 存档 98 行里 76 行的 patch 互相对不上(中位分歧 76%),而活下来的 22 行还从 0.39 跳到 1.59——**所有 patch 一致地读小**,因为它们一起混叠。两行同一个 run 给出机制:`382.77` 和 `126.76`,差 **256.01**。

> ⚠ **`max_abs_frame` 看不见它。** 混叠的那一对报出来是个**小**数,所以那个本该抓「帧间位移过大」的统计量,正是混叠藏身的地方。对得上的行和对不上的行,`max_abs_frame` 中位数是 91 和 92 px。

⚠ **由此推出的「1.5474 是上下两个方向的混合、该用 `K_down`」整条论证作废**:干净测出来上下不对称是 **0.19%**,不是 6.32%。**常数确实该往那个方向动,但理由是另一个——这是「对」得最差的一种。**

### 二之一、相关器的逐对偏差是**一条会变号的曲线**,而实弹梭正好骑在零点上

`tools/probe_correlator_bias.py --grid`,两个独立 run 各 10 轮 × 12 格。每格把一个已知总量铺在已知对数的帧上,于是那一格的**逐对位移 δ** 是设出来的:

```
total_px(格) = counts · K_true + n_运动 · b(δ)
```

**`b(δ)`,两个 run(A / B):**

| δ px | b px/对 (A) | b px/对 (B) | |
|---|---|---|---|
| **0** | +0.0002 ± 0.0003 | **+0.0000 ± 0.0001** | 静止对照,什么都不注入 |
| 0.6 | +0.0385 | +0.0561 | |
| 1.1 | **+0.0643** | **+0.0875** | 峰 |
| 2.0–2.2 | +0.037 / +0.044 | +0.032 / +0.041 | **实弹梭中位数在这** |
| 3.8–4.4 | −0.111 / −0.095 | −0.089 / −0.095 | 已经翻负 |
| 7–8 | −0.139 | −0.277 | |

**小位移超读、大位移欠读,在 δ ≈ 3 px 穿零。** 两端各有一个叫得出名字的机制(亚像素峰锁 / 帧内运动模糊),中间那个零点**不是拟合出来的,是两个 run 各自穿过去的**。

**三样复现了:**

| | run A | run B |
|---|---|---|
| **静止场景不漂** | +0.00018 ± 0.00027 | +0.00001 ± 0.00006 px/对 |
| **`b` 只由 δ 决定** | 0.6 σ / 0.5 σ AGREE | 0.2 σ / 0.8 σ AGREE |
| **两个候选模型都不成立** | χ²/dof 395 / 402 | 1085 / 940 |

第二行是这个设计里唯一**拟合安排不出来**的检查:两格取同一个 δ、对数差一倍、总量差一倍,而 δ 相等强制 `counts/n` 相等,于是 **`K_true` 在这个比较里精确抵消**。四次比较全部 ≤0.8 σ。

⚠ **由此,原来记在 `config.py` 里的「相关器每对超读 ~0.04 px,+7.54%」要收窄:** 那是曲线在 **δ ≈ 0.6** 那一端的值,由一条 1.5 s 铺开的臂测得。它是真的,但**它不是一个常数,也不适用于一梭**——一梭的 δ 分布(p25 0.90 / 中位 2.00 / p75 3.78)横跨零点,正负大量对消:

```
全库 272 梭逐对代入 b(δ)     run A  −0.28%     run B  −0.81%
```

**不是 +7.5%,也不是 +1.2%,是 −1% 以内,而且是负的。** §13 原来那个「固定还是比例」的二选一,两个选项都被拒了。

⚠ **而 `K` 本身没有复现,所以不动它:**

```
K_true(单对臂外推)     1.5459 ± 0.0010   →   1.5374 ± 0.0009    −0.55%
K_eff(实弹梭 δ 加权)    1.5416            →   1.5250             −1.08%
config 现存             1.5413            —— 被两个 run 夹在中间
```

**run 内的 sem 是 0.06%,run 之间挪了 0.55%——差 9 倍。** 按第零节,这**不是 `K` 漂了**:它是一次未定位的测量错误,而这个探针那一晚正好带着四个已知的(顶到俯仰上限、抓帧卡死、腰射尺子、臂时共变)。四个步长(20/35/50/70)在**同一个 run 内**一致到 ±0.1%,所以第十三节的②在方法上过了;**那个值先不动,直到两次读数在同一根尺子下对上。**

> **一条曲线的形状可以复现,它的标高不一定。** 这两件事要分开报,因为能进 `config.py` 的只有后者。

---

## 三、两条臂那道闸:**插花是必要条件,复现才是判据**

`y_true` 与当时播的是哪条曲线无关——这是拟合器安排不出来的那一条,也是这个模型唯一一道外部检验。

**它现在最强的一次通过**(2026-08-08,同一 run,逐梭轮换,补偿量三档):

```
曲线 counts    n   y_true(2.40s)   sem    视角位移
       0       5       853.8       5.7      853.8
     473       5       853.5       3.9      428.8
     945       5       850.4       6.2       10.1
                                            位移跨 85 倍，一致到 0.4%
```

**三条规矩,每一条都是被一次失败买来的:**

| | |
|---|---|
| **必须插花** | 跨场次比同一个问题给 +6.41%,插花给 −0.40%。插花不是为了「抵消漂移」(没有漂移),是**让两条臂共用同一次测量的一切未知**——尺子、姿态、视角所在的段,全都一样 |
| **必须复现** | 一次插花的 4.7 σ 结果被一次复现推翻(见一之七)。**插花保证「同一场次」,不保证「同一场次内没有结构」** |
| **分臂按「交付了多少」,不按 `comp_enabled`** | `--scale-sweep=0` 的梭 armed 着 174 个全零的结,`comp_enabled` 是 `True` 而 `y_true = y_obs` 精确。那个旗标答的是「固件上没上膛」 |

⚠ **闸门自己报告两条臂在时间上重不重叠,不重叠就打印 `NOT A VERDICT`**(`pixi run cube`)。它不猜哪个读数对,只拒绝把跨场次的数字当成臂的差别端出来。

## 四、⚠ 底噪:**逐梭 3% 是枪打出来的,而那个「2.7%」是个混杂**

> **2026-08-08 修正。** 这一节原来的结论是「每个 run 一个常数,加梭数消不掉」,而**「场次」不是一个模型,是给无知贴的标签**——如果 `y_true` 真的每个场次自成一体,那拟合一条曲线隔天播这件事从根上就不成立。
>
> **控制住「当时播的是哪条曲线」之后,它消失了。** 全文在四之二。下面的表是原始测量,一个字没改;变的是它说明什么。
>
> ⚠ **而「臂」本身分两个量,这一节当晚在这里栽了一次:**
>
> | | 是什么 | 现状 |
> |---|---|---|
> | 臂间的**散布**差 | 空臂散得比补偿臂开一倍 | ✅ 它**只落在靠测量的那条臂上**,所以在尺子里,不在枪里。四之二 |
> | 臂间的**均值**差 | 空臂比补偿臂低 2.16% | ⚠ **不是 `K`**,四之二之二。它是 `η` 或者 `y_comp` 积分算多了,而这两个分不开 |
>
> **我把这两个当成了同一个东西,并且用第一个的解释去预测第二个——差了 5.8 σ。**

2026-08-08 一晚上 123 梭 mp5k bare,全部按 `K = 1.5413` 重算,在 `t = 2.40 s` 上读,按 5 分钟间隔切成 9 个 run:

```
run   n   起始      跨度    mean    sd    CV      run 内趋势
  0   5  143749   1.0m   818.9  21.3  2.6%    +37.3/min
  1   3  152630   0.5m   804.1  50.2  6.2%   +187.3/min
  2   3  153244   0.5m   840.3  15.8  1.9%    +52.4/min
  3   5  172911   1.0m   861.0   8.4  1.0%     -1.3/min
  4  45  180458  19.7m   876.5  38.0  4.3%     -0.3/min
  5  15  190628   3.3m   866.6  37.8  4.4%    -13.6/min
  6  16  192051   3.8m   857.4  17.9  2.1%     -1.6/min
  7  16  195656   3.8m   845.9  24.8  2.9%     +5.3/min
  8  15  205327   3.5m   844.6  12.1  1.4%     -2.6/min

run 之间 mean 的散布   23.0 counts = 2.72% sd     全距 9.00%
旧表里的「慢漂」876.5 -> 844.6  ← 也是臂：后段混着空臂
```

**三个量,而第二个是这一节存在的理由:**

| | |
|---|---|
| 逐梭散布 | CV **2–4%** —— 枪自己的逐发随机(见四之一),`sem = CV/√n` 对它有效 |
| ~~run 之间的偏移~~ | ~~sd 2.72%~~ —— **那 9 个 run 里前三个是纯不补偿臂(均值 821.1)、中间四个是纯补偿臂(865.4),差 5.1%。这张表把臂读成了时间。** 见四之二 |
| run 内的斜坡 | 20 分钟的长 run 上 −0.3 counts/min,**基本没有** |

### 四之一、那个 2–4% 里,尺子只占 **≤0.5%** —— 所以它是枪,不是我们

`probe_correlator_bias` 的格子顺带给了逐对噪声,而且是**在实弹梭自己那个 δ 上**量的(δ≈2.1 px 那一格,20 次重复,两个 run):

```
每对帧的散布       0.25 / 0.31 px       ← 还含注入抖动，一梭没有那一项，所以是上界
一梭 478 对的 cumsum   0.28 × √478 = 6.1 px
一梭总量           ~1256 px
                   ------------------------
尺子的贡献         ≤ 0.49%      对着实测的 2–4%
```

方差上占 **1.5% 以内**。静止对照更小(逐对 0.003–0.013 px)。

> **那个 2–4% 是枪打出来的,不是我们量出来的。所以把相关器做得更准,一分钱都买不到。**

这条**取消了一类工作**:任何「提高单梭测量精度」的改进都对底噪无效,唯一的杠杆是**多打几梭**(而 `sem = CV/√n` 对这一项确实成立)。

⚠ **但这一节说的是「尺子的随机噪声」,不是「尺子的标度」。** 相关器逐对抖动 0.5%(随机、可平均掉)和一个**乘在 `y_obs` 上的系统性标度错**(平均不掉)是两个完全不同的东西,排除了前者不等于排除了后者。**上一版这里写着「台阶不可能是尺子的」,那句话把两者当成了一个。**

### 四之二、拆开之后:**枪没变,多出来的那份在尺子里**

判据是数据自己给的,不需要新跑:**同一条曲线跨场次比 = 场次的影响;同一个场次跨曲线比 = 臂的影响。** 而库里大半场次本来就是插花的(`0808_205327` 打 0/470/940,`0808_195656` 打 0/950,`0808_023500` 打 0/1520……)。

在 `t = 2.40 s` 上读,按 (场次 × 臂) 分格,拿**组内**逐梭 CV 算出抽样噪声该有多大,再跟**组间**实测比:

| | 抽样噪声预测 | 实测跨场次 sd | 比值 |
|---|---|---|---|
| mp5k **补偿臂**(9 组) | 1.09% | 1.28% | **1.17×** |
| m416 **补偿臂**(7 组) | 0.87% | 0.93% | **1.07×** |
| mp5k **不补偿臂**(5 组) | 1.23% | 2.53% | **2.06×** |

**前两行就是抽样噪声,没有残余。所以 `y_true` 没有场次项——枪就是枪。**

多出来的那一份只落在不补偿那条臂上,超出部分 `√(2.53² − 1.23²) = 2.2%`。而两条臂的区别只有一个:

```
不补偿臂   y_true = y_obs                 答案 100% 来自测量，|y_obs| = 830 counts
补偿臂     y_true = y_obs + y_comp        答案几乎全来自已知曲线，|y_obs| =  36 counts
```

**一个乘性的测量误差,乘在 830 上是 18 counts,乘在 36 上是 0.8 counts。** 这正好解释它为什么只在一条臂上露头,而且**只有乘性误差能这样**——加性的会两条臂一样大。

**那份多出来的量,只落在「答案全来自测量」的那条臂上。所以它在尺子里,不在枪里**——这正是第零节那条前提预言的形状,也是这一节最强的一句话。

⚠ **它叫什么,还不知道,而且不许叫「漂移」。** 我曾经把它命名成「`K` 每场次漂 1–2%」,再拿这个名字去预测臂间的**均值**差——**差了 5.8 σ**(四之二之二)。散布和均值是两个不同的量;而给一个未定位的分歧起个名字,并不会让它变成一项机制。

### 由此得到的模型

```
y_true(t)   枪的属性，固定，永不改变          ← 第零节的前提，要拟合的东西
测量误差     乘在 y_obs 上，来源未定位        ← 一个待查的 bug，不是模型的一项
```

**所以采集要打补偿臂,不是空臂。** 而理由**不是**「让某个漂的量被乘小」——是**补偿臂的答案几乎全来自那条已知的曲线,只有很小一部分经过尺子**。`|y_obs|` 从 830 降到 36,任何乘性的测量错都被乘小 23 倍。

> **这是一个零位测量:越接近答案越精确。** 而且它对「那个错到底是什么」完全不敏感——所以这条结论不随第零节的清理动摇。

### 四之二之二、⚠ **臂间的均值偏移不是 `K`** —— 一个开火前写下的预测,失败了

上一节把「空臂散得更开」归给 `K`,然后我把同一条推理推到了「两条臂的**平均值**差多少」上。那给出一个可证伪的预测,**而它是在开火之前写下来的**:

```
若臂间差全部来自 K 错 r 倍:    OFF − ON = y_comp · (r − 1)
```

于是一局之内做两件事:**打插花梭 + 用 grid 探针独立量 `K`**。

**实测**(2026-08-08 22:28,mp5k bare,一局之内,逐梭交替,8 对 8):

```
arm    0    n=8   mean 837.37   sd 4.59
arm  900    n=8   mean 856.79   sd 7.58
            OFF − ON = −19.42 ± 3.13   6.2 σ   = −2.16% of the 900-count curve

同一局的 grid   K_true 1.5372 ± 0.0003   K_eff 1.5394  →  预测 −1.11 counts   差 5.8 σ
```

**预测差了 5.8 σ。`K` 解释不了臂间的均值偏移。**

⚠ **而 `K` 这次是被钉死的,不是估的**:`K_true` 的 sem 是 0.0003(0.02%),要产生 −19.42 counts 需要 `r − 1 = −2.16%`,也就是 `K = 1.508`。**那和实测差 65 个 sem。**

把 `K` 代回完整的恒等式,剩下的那个未知数就孤立了:

```
OFF − ON = y_comp · (φ·r − 1)        r = K_real/K_stored = 0.99877（实测）
→ φ = 0.9796
```

**曲线只到了 98.0%。这就是 `η`。**

### ⚠ 所以 `η` 从「假的」那张表里回来了,而它有三次记录

| | φ | |
|---|---|---|
| run A 19:57 | 0.971 | 4.7 σ |
| run B 21:2x | **1.004** | 不显著,三档、位移跨 85 倍 |
| **run C 22:28** | **0.980** | **6.2 σ,而且 `K` 被独立仪器钉住** |

三次里两次说 2–3%,一次说 0。**上一节写的那个「`η` 的死因」(说它测的量被一个同样大小的 `K` 误差污染)因此也是错的**——`K` 现在量出来了,它没那么大。

> **`η` 现在的状态不是「真的」也不是「假的」,是「三次里有两次显著、而第三次用最强的杠杆说没有」。** 它回到第十三节,不进正文。

### ⚠ 而有一样东西跟 `η` **在这个测量里完全不可区分**

`OFF − ON` 那个差可以来自两件事,这次测量分不开:

| | |
|---|---|
| **曲线没全到**(`η`) | 命令 900 counts,实际到 882 |
| **我们把曲线算多了** | 曲线到齐了,而 `y_comp` 的积分多算了 2% |

第二个就是第十三节的**第 6 条**:`comp_counts_at`(PC 这边怎么以为固件在结之间插值)和 `hold_s` 处的冻结,**两条都只读过 C 代码,从来没有独立测量**。2% 的积分误差产生的符号和幅度**一模一样**。

**所以第 6 条从「地基没验过」升级成「它决定 `η` 是真是假」。** 在它之前,任何关于 `η` 的结论都还差一个它分不开的兄弟。

⚠ **顺带记一条方法**:这一局的**前**一次 grid 读数(`K_eff = 1.5308`)作废了,因为在它和开火之间游戏掉回了大厅——那是一个新场次。**拿一个已结束场次的 `K` 去配下一局打出来的梭,正好是这一节在拆的那个混杂。** 作废它比用它便宜。

### 四之三、⚠ **形状**上还剩 0.55%,而按第零节它只能是测量残差

同一次分析顺带量了形状(每条曲线除以自己的总量,所以纯乘性的东西在这里**恰好消失**):

```
mp5k  arm 0      3 组   实测 0.60%   抽样 0.22%   超出 0.56%   = 5.1 counts
mp5k  arm 900    5 组   实测 0.57%   抽样 0.14%   超出 0.55%   = 5.2 counts
mp5k  arm 1000   2 组   实测 0.38%   抽样 0.17%   超出 0.34%   = 3.2 counts
```

两条臂一样大,而乘性的东西在归一化里被约掉了——**所以它不是那个乘性的测量错,是另一个。**

⚠ **它不叫「形状的场次效应」。** `y_true` 的形状按第零节是固定的,所以这 0.55% **不描述枪,它描述我们**:一个**分组间残差的上界**,量级 5 counts / 940。

**记在这里是当上界用的,不是当解释用的。** 下一个想解释「为什么隔天差一点」的人应该先看到这个数,而不是重新发现一遍——然后去找那个错在哪,而不是给它起个名字。

### ⚠ 没坐实的部分

- **空臂那一行只有 mp5k、5 组**(m416 的空臂组不够 3 个),而且那 5 组**全是早期场次**——在那一侧,臂和时间仍然部分共变。要钉死它需要**在一个新场次里插花打空臂和补偿臂**,`--scale-sweep` 已经能做。
- **m416 的补偿臂只有 3 组,比值 1.50×**,闸门没有判它通过而是明说组数不够。**mp5k 那条 ✅ 目前是唯一一条干净的。**

跑法:`pixi run session-split`(纯离线)。

> **所以「同一次测量之内」这个条件保护得了插花的两条臂,保护不了两次测量之间的任何比较。** 不是因为中间有什么在漂,是因为**两次测量之间,那些未定位的错可以取不同的值**。

### ⚠ 方法论结论,比数字重要

> **run 内的 σ,预测不了复现性。**

一个在 run 内 **4.7 σ** 的效应——插花做的、同一场次、逐梭交替——被一次复现推翻。**存在一个 run 内统计量看不见的误差分量,量级 2–3%,而它在两次测量之间可以取不同的值。** 按第零节,那是一个还没定位的 bug,不是系统的一个性质;而 2026-08-08 那一晚一口气抓出四个够大的候选。

```
旧判据:  σ 够大                     -> 是发现
新判据:  σ 够大  且  换一个 run 还在  -> 是发现
```

### 「无解释」的分歧,历来都是这个尺寸

```
旧表里读成「run 之间的台阶」的      2.72% sd   ← 其实是臂，见四之二
M.K 那格两条流水线                  7.6%      ← 隔三天测的，不再是悬案
```

**两条都查到了根因,而且都不是漂移。** 这本身就是第零节那条前提到目前为止的记录:**每次认真找,找到的都是一个错,不是一个性质。**

## 五、对策

### ① 立方体的八格必须**在一个 run 里轮转**,不能一格一个 run

**四之二把这条的理由从「经验」升成了「精确抵消」:** 那个未定位的测量错是**乘性的**,而同一次测量之内八格共用同一份。所以做**比值**(配件因子恰好就是比值)时它**约掉**——不是被平均小,是消失。⚠ **这条只需要「它是乘性的、并且这一轮之内不变」,不需要知道它是什么。**

一格一个 run 则相反:每格乘一个自己的 `K`,而配件效应本身是 5–25%,`M.K` 那 7.6% 就是这么来的。

⚠ **轮转之后可以做配对比较**:每一轮打全八格,轮内的差**不带 run 间偏移**。同样的梭数,精度差一个量级——比「每格多打几梭」有效得多。

⚠ **代价转移到换配件的时间上**,所以轮转的粒度(每轮八格 / 每轮两格)由「换一次配件 vs 打一梭」的耗时比决定。那个比值还没量过。

### ② 每格几梭由 CV 定,不是拍脑袋

CV 3% 下 `sem = 3%/√n`:

```
n=3  1.7%      n=5  1.3%      n=9  1.0%      n=25  0.6%
```

### ③ 能分辨多少,写死在这里

| | |
|---|---|
| 同一 run 内、插花、每臂 n=8 | sem 1.1% → **3% 的真效应看得见** |
| 同一 run 内、比**值**(配件因子) | `K` 精确约掉,只剩抽样噪声 |
| **跨 run,补偿臂** | 抽样噪声就够了(实测 1.17× / 1.07×),**没有额外的场次罚款** |
| **跨 run,空臂** | 额外 **2.2%** 的乘性罚款(一个未定位的测量错,四之二) |
| 任何单 run 的结论 | **必须复现,否则不算** |

⚠ **上一版这里写着「跨 run 5% 以下的差全都不可信」,那是把臂当成了 run。** 补偿臂跨 run 是干净的;贵的是空臂。

### ④ 闸门要加一项:**复现**

一个只在一个 run 里成立的效应,不进 `config.py`、不进 `MODEL.md` 的结论区,只进「待复现」。**这一条是 η 买来的。**

---

## 六、它消掉的东西 —— 不是做得更准,是不再存在

| | 弹号坐标 | 时间坐标 |
|---|---|---|
| 开火频率 | `span/(n−1)`,进桶边,5% 误差累积成 2 发 | **不出现** |
| 弹桶归属 | 第 k 发的位移必须记进第 k 个桶 | **没有桶** |
| 两个原点 / `W = 13 ms` | 弹药计数器 vs 点击,差值只在 AUG 上量过一次 | **只有点击,而且是我们自己发的** |
| 游戏输入延迟 `L` | 未知,不敢碰 | **被曲线自己的形状吸收** |
| `np.interp` 的 `right=` 钳位 | vss 末发 ±100 counts 靠它翻硬币 | **不存在** |
| 收敛判据 | 整格均值瞎、首末梭、尾窗——三次改判据 | **不存在**,拟合是一次性的 |

⚠ **`L` 被吸收有一个前提:网格要能表示平移。** 见第四节。

---

## 七、表示:采样点 → 全留 → 聚类 → 最大簇 → 拟合

```
每帧一个样本  (t, y_obs)
      │
      ├─→ 落盘,永不删除,带上当时在播的曲线
      │
      ▼
  y_true 样本 = y_obs + y_comp        每梭一条轨迹
      │
      ▼
  聚类(单位是**整条轨迹**,不是单个点)
      │
      ▼
  最大簇 ──→ 拟合 ──→ 194 点 × 17 ms ──→ 上传
```

### 聚类的单位是一整梭,不是一个点

**总会有离群点,而它们几乎从不是散的。** 让一梭报废的东西——手动了、姿势不对、配件没装上、开火中途掉出 ADS、相关器跟丢——**污染的是整条轨迹**,而不是轨迹上零星几个点。

而且被污染的那条轨迹**看起来完全合理**:光滑、单调、量级正常。逐点的离群剔除**永远抓不到它**,只有把它和别的梭放在一起比才看得出来它整个偏在一边。

所以:

1. 每梭的 `y_true(t)` 重采样到公共网格 → 一梭变成一个向量
2. 在这些向量上聚类
3. **取最大簇**,只用它拟合

⚠ **阈值必须从数据里出来,不许硬编。** 这个仓库里每一个拍脑袋的阈值最后都成了一道只验过一侧的闸(`MARGIN_MIN` 拒 0%、`ADS_FRAC_MIN` 55 梭全是误拒)。聚类的距离标度要由簇间间隔本身给出。

⚠ **被丢掉的梭要记下来,连同它为什么落在别的簇里。** 一道说不出自己拒了什么的闸,改不动也验不了——根 `CLAUDE.md` 里那条「闸门会审查掉你用来重调它的数据」在这儿一字不改地成立。

---

## 七之二、`y_obs(t)` 怎么量:锚住不动,别攒

相位相关只能在**有共同内容**的两帧之间做。所以「和第 0 帧比」能不能成立,取决于**窗口盖不盖得住位移**。量出来是盖得住的:

```
red_dot 半个 patch 窗口(过半相关峰会 wrap)    128 px = 82.7 counts
一梭累计位移 |cum_px|      p50      98 px     ← 比窗口还小
                           p90     415 px
                           max    8682 px     (裸枪 / 坏梭)
```

**中位数那一梭整梭走完都没出一个窗口。** 压好的梭里第 0 帧就是全程有效的参考。

⚠ **而现在的实现是逐帧差分再 cumsum 约 300 次——它把一个 1 步的直接测量变成了 300 步的随机游走。** 不是精度不够,是白送出去的。

所以:

| | |
|---|---|
| **主路** | 锚在第 0 帧,一直用,直到位移逼近半个 patch |
| **换锚** | 只在逼近时换,**并记下换在哪**。p90 那类梭约 4 次,裸枪几十次 |
| **逐帧链** | **保留**,只当**交叉校验**——两条路算同一个量,不一致就是抓到了错 |

最后一条不是冗余癖:这个仓库的历史全是「自洽的错数」,而**两条独立路径给同一个量**是唯一能抓住它的东西。代价接近零——相关是开火结束后离线跑的(`MagazineRecorder` 先攒 patch 再做 FFT),不占帧预算。

⚠ **逐帧也不是永远安全**:`|dy|` 每梭最大值 p99 = **89.4 px**,已经是 128 px 窗口的 70%。`out_of_range` 那道闸两条路都要。

### 这也消掉了「帧率越高噪声越大」那个权衡

逐帧 cumsum 下,帧率 ×2 ⇒ 累加步数 ×2 ⇒ sd ×√2,**165 Hz 是亏的**。

锚住不动之后,**累加次数 = 总位移 / 窗口,和帧率无关**。于是提帧率只买时间分辨率,不付噪声。**窗口高度因此成了一个真的旋钮**:patch_h 从 256 加到 512,换锚次数减半,而抓取从 0.45 Mpx 涨到 0.9 Mpx——仍在 6.06 ms 的预算里。

⚠ **「每对噪声是恒定的还是正比于位移」这个问题现在答不了:逐帧数组从来没落过盘**,3073 梭的记录里只有 `mean_mad` 这类聚合量。这正是本模型要修的那件事,而它连自证的数据都没有。

---

## 八、固件那一侧(2026-08-08 读码确认)

| | |
|---|---|
| 播放锚点 | `fire_start_ms`,即**点击** |
| 分辨率 | **1 ms**(`get_recoil_delta` 每毫秒被调一次) |
| 节点间 | **线性摊开**——`spread_dy_per_ms = dy / dur`,`dur` = 到下一个节点的时间 |
| 容量 | **300** 个节点(`MAX_PATTERN_POINTS`,host `MAX_POINTS` 与之匹配) |
| 抖动 | ±2% 幅度 + ±0.2 count,**零均值**,只为打散完全可复现的模式 |

**所以固件本来就是一个时间域的折线播放器,正是这个模型要的东西。** 挡在中间的只有一处:

> `press/pico_mouse.py: upload_pattern()` —— docstring 第一句就是 “Upload pattern merged to **one point per bullet**”。它拿 `bullet_interval_s` 把任何细网格重新合并回一发一个点。

### 42 个节点的真实代价(不是「一坨」)

⚠ **曾经写成「42 个 85 ms 的大坨」,那是错的,已收回。** 累计曲线在节点之间是**线性**的,所以 42 节点 = 一条 42 段的折线。

代价在**段内**:AUG 一个 88 ms 间隔内的踢腿(补偿关掉,折叠到一个间隔上,12 个采样),原测于 `tools/probe_kick_profile.py`——⚠ **那个探针 2026-08-08 随弹桶坐标删了**(它 import `calibration.analysis`),**数本身还活着**:同一组值抄在 `press/pico_mouse.py` 的 `upload_pattern` 注释里,那是它现在的出处:

```
0.9  1.3  0.7  2.7  2.5  2.3  2.2  1.9  1.6  1.2  1.2  1.4
```

**间隔内速率变化 4 倍**,而 85 ms 一段的折线在段内**速率恒定**。拿恒定速率去追这条剖面,准星在每个间隔内来回漂 **±10% 一发的量**,而 42 节点的曲线**一点都去不掉**——它没有那个自由度。

17 ms 的网格把这个剖面切成 5 段,能追。

---

## 九、采样:165 Hz 是硬顶,现在拿到 53%

Desktop Duplication 给的是合成器**已经合成**的帧,所以上限就是刷新率(实测 **165 Hz / 6.06 ms**)。

拿不满的原因是每帧抓了逐帧用不上的东西,而 **DXGI 只能有一个 bounding box**:

| 每帧抓什么 | 区域 | bbox | 耗时 | 占 6.06 ms |
|---|---|---|---|---|
| 现在(patch + 弹药 + 火力模式 + 枪名 + 配件槽) | 23 | 1911×1244 = 2.38 Mpx | 5.41 ms | **91%** |
| **只要 tracker 的 patch**(red_dot) | 7 | 1768×256 = 0.45 Mpx | **1.62 ms** | **27%** |
| **只要 tracker 的 patch**(vss_pso1) | 3 | 873×256 = 0.22 Mpx | **0.95 ms** | **16%** |

**时间坐标下每帧只需要 patch。** 弹药、火力模式、姿势、枪名一个都不进逐帧路径——它们回答的是「这一梭算不算数」,那是每梭一次的问题。

165 Hz 对 17 ms 的固件步长是每步 2.8 个样本。**而相位是白送的**:点击落在 vsync 哪个相位每梭都不同,10 梭叠起来样本自己在 6 ms 的格子里抖开,亚帧分辨率不用争取。

---

## 九之二、合并假设在中段站得住,两头要单独解释

四条强度差 3 倍的曲线,`y_true` 在 **t ≤ 2.4 s** 上极差 < 6%,t=1.5 s 上 < 1%。**那正是第三节要的东西。**

要单独解释的是**两头**:开火起始约 0.3 s 的时序,和**过量补偿在晚期的塌陷**——后者有位置(t≈2.5–2.7 s)、有条件(只在 x1.5)、可复现。

### 两条推论

1. **不能指望一次拟合到位。** 要迭代:拟合 → 上传 → 重采 → 再拟合。第二轮起 `y_obs` 就在平台区里。
2. ⚠ **关掉补偿那一臂不是无偏的锚。** 它的 `y_true` 在 t=3.8 s 上是全场最低,比平台低 13%。**拿它当基准,是拿偏得最狠的一点当基准。**

## 十、这一节里哪些是量出来的

**量出来的**(2026-08-08,全部可复查):

- 固件的播放语义(第四节整表)、`MAX_PATTERN_POINTS = 300`
- `upload_pattern` 合并到一发一点
- AUG 的弹间隔内踢腿剖面(`pico_mouse.py` 的注释里存着)
- 165 Hz / 6.06 ms;第五节的抓取耗时表
- 循环实测 **88 fps 中位**(3131 梭的记录)
- **220 个格子的 `sum|sys| / sum|e|`:p10 0.54 / p50 0.78 / p90 0.96** —— 逐发误差里逐梭可复现的那一份。大头是偏差不是噪声,但中位仍有 22% 不可复现
- ~~桶边是 `np.arange(nb+1) * interval`,原点是弹药数字第一次变化~~ —— ⚠ **出处 `calibration/analysis.py` 2026-08-08 删除**。这一条是对**被这份文件取代的那个实现**的描述,它随实现一起退场;留在这里是因为第二节那张表引用它。**没有任何东西能再复核这两个行号了**,这是删掉实现时清醒付掉的代价。
- 逐发的出膛时刻**每帧都在算**,只留首尾和一个计数([fire.py:398-405](control/fire.py#L398-L405))
- 帧时间戳盖在**阻塞等待之前**([fire.py:392-396](control/fire.py#L392-L396)),标的是它自己都还不存在的那一帧
- `AcquireNextFrame` 的 `LastPresentTime` 到了进程里、三行后被丢掉(bettercam `core/duplicator.py:22`)
- `scar_att` 步长 100 ms 对实测火循环 92.26(**后半梭差 3.19 发**)、`ump45_att` 92 对 89.05(**1.19 发**)

**没量的,是一句预测:**

> 粗网格**是**那份可复现偏差的成因。

**这条一个字的实测都没有。** 在新模型跑出第一格、`sum|sys|` 真的掉下来之前,它是待验假设。这个项目 2026-08-07 一晚上因为把假设当结论撞了六次同一个形状,而这一条现在**长得跟结论一模一样**。

⚠ **两条我自己在同一天里写错又收回的,留着当样本:**

1. 「220 格 sys/e = 0.93–0.99」—— 那是**把按 `sum|sys|` 排序的前 14 行当成了分布**。全量中位是 0.78,166/220 低于 0.90。**读了有序表的头部就当成总体**,同一天第二次。
2. 「42 个 85 ms 的大坨」—— 固件在节点间是线性摊开的,它是折线不是台阶。**在读固件之前就断言了固件的行为。**

---

## 十一、旧坐标下的哪些法则仍然有效

**只要 `analyse()` 还在按 `arange(nb+1)*interval` 分桶,下面这些就还生效**,它们跟着实现一起退场,不是现在:

| 法则 | 在哪 | 时间坐标下 |
|---|---|---|
| 对角约束(第 k 发的偏差修第 k 发) | 根 `CLAUDE.md` | **不变**,只是「位置」从弹号换成时间 |
| 观测对齐必须验(尖峰打在第 k 发,看哪发动) | `detector/CLAUDE.md` 第四铁律 | **退场**——没有桶就没有错桶 |
| 两个原点 / `W = 13 ms` | 同上 | **退场**——只剩点击一个原点 |
| 收敛判据要有序(首末梭 / 尾窗) | 根 `CLAUDE.md` | **退场**——拟合是一次性的,没有轮次 |
| 不许平滑、不许跨项 | 根 `CLAUDE.md` | **不变**,而且更强:聚类的单位是整梭,不是把邻居摊开 |
| 闸门要两侧都验、要记下自己拒了什么 | 各处 | **不变**,直接适用于聚类 |

---

## 十二、跨配置怎么合并 —— **不合并。存整条,永不拆**(2026-08-08 实测)

配件因子那套东西在时间坐标下的对应物,这一节以前是空的。

### 规矩

> **每个配置存自己那一条完整曲线。绝不拆成逐槽的乘数。**

理由不是「怕不准」——下面的实测说它相当准。理由是**出问题时你要分得清是哪一槽**:一条按 `(weapon, config_key, posture, sight)` 归档的曲线,是那把枪那套件**直接测出来的**;一个由三个因子相乘得到的数,错了之后没有任何东西能说出错在哪一项。要求原话:「保存差乘结果的那个,不要拆那个数字,不然你有问题你不知道加满」。

`samples.py` 本来就是这么做的(按 `config_key` 归档),这一节把它从「实现碰巧如此」变成「规矩」。

### m416 的 2×2×2,八格全测(comp OFF,红点,站姿,梭末 t=3.415 s)

```
cell    n    counts   f vs bare        M=comp_ar  G=vert_grip  K=tactical_stock
···     5   1459.5    1.0000
··K     5   1328.0    0.9099
M··     5   1135.6    0.7781
·G·     5   1119.9    0.7673
M·K     5   1041.0    0.7133
·GK     5   1041.9    0.7139
MG·     6    893.1    0.6119
MGK     5    810.6    0.5554
```

配对超出(实测 ÷ 单件相乘),20000 次 bootstrap 95% CI:

```
M+G     +2.5%   [ +0.4%,  +4.6%]      区间勉强不含 0
M+K     +0.8%   [ −2.3%,  +3.9%]      含 0
G+K     +2.2%   [ −1.5%,  +6.3%]      含 0
M+G+K   +2.3%   [ −2.4%,  +6.9%]      含 0
```

**没有三阶项**:拿实测的 MG 乘 K 去预测三件,差 **−0.2%**。

**所以 m416 上配件因子基本是相乘的,精度 ±4%。** 而 M+G 那条的下界只有 +0.4%,还是四次未做多重比较校正的检验——**当作「悬着」,不当作已确证**。

### ⚠ 但「一条曲线 × 一个数」是**假的**

因子是 `t` 的函数,不是常数:

```
t=0.3 s   comp_ar 0.9227      t=3.2 s   0.7675      跨 17%
```

配件对**后段**的帮助远大于对头几发。按各自末点归一后形状差 1.7–3.4%(约 20 counts),所以「同一条曲线」作为**近似**站得住,而**「乘一个数」在时间上是错的,误差跟耦合同量级**。

这正是「存整条」的第二个理由:一条实测曲线自带这个时间结构,一个标量丢掉它。

### mp5k 的 2×2×2,八格全测,而且**两条流水线各测了一遍**(`pixi run cube`)

这一格上一版写的是「⚠ 旧那张表里 mp5k 的非乘积**不能当作已复现**」。**现在复现了,而且是最强的那种复现**:`data/kit_factors.json` 是 08-05 用**已退场的弹桶坐标**、另一套代码、另一天测的同样八格,和今天时间坐标下的八格逐格对得上。

```
cell    n    counts     sd     cv  f(时间坐标)  f(08-05 弹桶)   差
bare   11     903.4   32.8   3.6%      1.000       1.0000     +0.0%
M..     5     518.7    3.8   0.7%      0.574       0.5907     −2.8%
.G.     5     669.5    8.2   1.2%      0.741       0.7470     −0.8%
..K     4     751.5    9.0   1.2%      0.832       0.8170     +1.8%
MG.     5     437.3    2.4   0.5%      0.484       0.4792     +1.0%
M.K     5     463.4    3.4   0.7%      0.513       0.4767     +7.6%   ← 唯一一格不合
.GK     5     538.5    7.5   1.4%      0.596       0.5907     +0.9%
MGK     5     395.1    2.1   0.5%      0.437       0.4216     +3.7%
                        M=comp_smg  G=vert_grip  K=heavy_stock
```

**八格里七格差 ≤3.7%。** 两条流水线要在同一个方向上同时错才能伪造出这个。

### mp5k **不是**相乘的,而 m416 是 —— 「槽位相乘」是**每把枪自己的性质**

```
配对            实测      单件相乘   超出      95% CI(20000 次 bootstrap,单位=梭)
muzzle × grip  0.4841    0.4255    +13.8%   [+11.0%, +16.4%]
muzzle × stock 0.5130    0.4776     +7.4%   [ +4.7%, +10.0%]
grip   × stock 0.5961    0.6164     −3.3%   [ −6.0%,  −0.7%]
三件全装        0.4374    0.3539    +23.6%   [+17.9%, +28.7%]
```

对比同一天同一套算法测的 m416:四条全部 ±2.5% 以内、三条含 0。

**所以不能说「配件相乘」也不能说「配件不相乘」。** m416 上相乘(±4%),mp5k 上 muzzle×grip 差 14%、三件差 24%。`control/kitting.py` 从 08-05 起就写着同一句话——「**『槽位不相乘』是 muzzle-grip 这条边的性质,不是模型的性质**」——今天在新坐标下拿到了同一个结论。

⚠ **`grip × stock` 的 −3.3% 别当已确证**:CI 上界只到 −0.7%,而且这是四次未做多重比较校正的检验里的一次。和 m416 的 M+G(+0.4% 下界)是同一个成色。**另外三条不是**——+13.8% / +7.4% / +23.6% 的下界分别是 +11.0 / +4.7 / +17.9,没有商量余地。

⚠ **`M.K` 那 +7.6% 是两条流水线唯一对不上的一格,原因不明。** 它同时也是 08-05 那边唯一被写成「+1.4 sigma,相乘」的配对。两条线各自内部自洽(cv 0.7%),所以这不是噪声。**记在这里,没有解释。**

---

## 十三、待验证 —— **这一节以外的东西都是验过的**

> 这份文件只留**验过的**。下面是没验过的,以及**验过并且是假的**。
> 任何一条要进上面的正文,必须:**测出来 + 换一个 run 复现**(见第三、第四节)。

### ⚠ 先按类型筛一遍 —— **不是所有「还没搞清楚的」都是模型问题**

「不能无限往下弄」是对的,而判据不是「还剩几条」,是**这一条属于哪一类**:

| 类型 | 该怎么办 | 现在有几条 |
|---|---|---|
| **恒等式里有一项从没被独立测过** | **必须做。** 这不是加输入,是确认已有的项就是我们以为的那个 | **1**(第 6 条) |
| **一个还没定位的测量错**(§0) | 允许,但那是**调试**,不是建模。它不进模型,进 bug 列表 | 1(5c) |
| **一个已经小于底噪的残差** | **记下上界,不追。** 逐梭底噪 2–4%,比它大 4–8 倍 | 1(5d,0.55%) |
| **一个只有在某功能上线后才承重的项** | 等那个功能存在 | 1(第 3 条,`human`) |
| **一个不进拟合的可调参数** | 不是模型问题,是调参 | 1(第 4 条,`D`) |
| **一个只在某个修正被启用时才要紧的界** | 用上了再说 | 1(2c) |

> ### 停止规则
>
> **当恒等式里每一项都被「读源码」以外的东西量过至少一次,而剩下的每个分歧都小于那个消不掉的底噪时,验证就完成了。**
>
> 按这条,现在**只差一项**:`C(t − M)` 那一项(第 6 条)。其余全部已经量过,或者已经小于底噪,或者根本不进拟合。

⚠ **而 `η` 从这张表里消失了,不是因为它解决了。** 它的三次读数互相矛盾(0.971 / 1.004 / 0.980),按 §0 的推理规则那意味着**至少两次是错的**——所以它不是一个待刻画的量,是**一个待解释的分歧**,而第 6 条正是它最像的那个错因。**先做 6;做完 `η` 要么消失,要么变成一个有唯一读数的量。**

### 还要验的

| # | 要验什么 | 为什么要紧 | 怎么验 |
|---|---|---|---|
| ~~1~~ | ~~相关器的累积偏差是固定还是比例~~ | **✅ 2026-08-08 答了,而且两个选项都是错的** —— 是一条 δ≈3 px 穿零的曲线,一梭净值 −0.28% / −0.81%。见二之一 | |
| ~~2~~ | ~~`K = 1.5413` 换一个步长复现~~ | **✅ 方法上过了**(四个步长同 run 内 ±0.1%),**而值没有**:跨 run 挪 0.55%。所以 `K` 不动。见二之一 | |
| ~~2b~~ | ~~`K_eff` 的分歧和弹梭那边的是不是同一个~~ | **✅ 大小对得上**:grid 两次读数差 1.08%,弹梭两条臂的方差差反解出 2.2%,都是乘性的。**合并进 5c,而 5c 现在是一次 bug 猎捕,不是一次刻画。** | |
| 2c | `b(δ)` 在 δ = 8–31 px 之间怎么走 | ⚠ **只在那个修正被启用时才要紧,而它没有。** 界是 −0.36% 到 +0.17%(3.38% 的对帧占 13.75% 的行程)。**今天没有任何东西读 b(δ),所以这一条不欠账** | 真要用 b(δ) 修正之前,补几格大 `counts` / 短 `spread` |
| 3 | **`human` 项的时间对齐** | 符号验过是对的,但 `\|slope\|` 只有 K 的 1/33 —— 逐帧相减只去掉约 3% 的手。⚠ **今天是休眠的**(全库 131146 个间隔里 2 个非零,机器打枪没人碰鼠标),**而任何「一边有人在玩一边观测」的东西一上线,它立刻承重** | 给 `human_totals` 带时间戳,或按帧区间积分 |
| 4 | `D` 在 [−19, −5] 之间定不下来 | ⚠ **不是模型问题。** `D` 是**选**的不是测的,而且第一节写着**它根本不进拟合**——写错了只在屏幕上多留残差。这是调参,归排期 | 只扫 −19 / −12 / −5,加大 n |
| ~~5~~ | ~~run 之间那 2.7% 是什么~~ | **✅ 大部分是混杂**:那九个 run 里臂和时间共变,而补偿臂跨组只有抽样噪声——**枪没变**。残下的归 5c | |
| ~~5b~~ | ~~新场次里插花打空臂+补偿臂,复现那 2.2%~~ | **✅ 打了,而且结果比预期大得多**:臂间差 −2.16%、6.2 σ,**同一局的 `K` 解释不了它**(差 5.8 σ)。见四之二之二。它没有复现 `K` 那个归因,它**否掉了**那个归因 | |
| **6** | **`comp_counts_at` 复现固件、`hold_s` 处补偿冻住** | ⚠ **升级成决定性的一条。** `C(t−M)` 是恒等式一整项,只读过 C 代码;而**积分多算 2%** 和 **`η`=0.98** 在四之二之二那个测量里**符号和幅度完全一样**。不分开它,`η` 的真假就定不了 | 上传已知曲线,量屏幕对照分段线性模型;特别要量**释放之后**那一段(`hold_s` 冻结) |
| ~~η~~ | ~~曲线到底到了多少~~ | **不是一个待测的量。** 三次读数 0.971 / 1.004 / 0.980 互相矛盾 ⟹ 至少两次错了(§0)。**它是第 6 条的一个症状**,不是一个平行的条目 | |
| **5c** | **那个 1–2% 的乘性测量错,在哪** | ⚠ **按第零节它是一个 bug,不是一个性质**,所以这一条是**猎捕**,不是刻画——「它有多大」不是答案。已知的候选:视角走到俯仰非线性段、抓帧丢帧、腰射尺子不平、patch 落在低纹理上 | 每个候选**单独造出来**,看它能不能复现那 1–2%;造不出来就往下一个 |
| 5d | ~~形状上那 0.55%~~ | **上界,不追。** 5.2 counts / 942 = 0.55%,而逐梭底噪是 2–4%——**它比消不掉的那个小 4–8 倍**,追它不改变任何一条曲线 | 记在四之三。哪天底噪降下来了再回来 |

⚠ **「换一次配件 vs 打一梭各占多久」不在这张表里,是故意的。** 它决定第五节①轮转的粒度,但它**不是模型的一条假设**——测错了标定会慢,不会错。这张表只收「错了会让模型说假话」的东西。那个数记在第五节①,归排程。

### 验过并且是**假的** —— 不要再推导出来一遍

| 假设 | 怎么死的 |
|---|---|
| ~~**`η`:曲线只有 96–97% 到达屏幕**~~ | ⚠ **2026-08-08 深夜从这张表里回去了。** run C 在 `K` 被独立钉住的情况下给 φ=0.980、6.2 σ。它现在在「还要验的」那张表里,不在这里 |
| 曲线因此比真实后坐力大 4.3%;不动点让准星恰好正确 | **这一条仍然是假的**,它是从 φ=0.957 那个值推出来的,而三次记录里没有一次给出那个值 |
| **SIZE**:游戏对「很多个 1-count 报告」处理不同 | 240 个 1-count 的 `mouse.move()` 给满 K(1.5357 对 1.5128) |
| **PATH**:固件曲线播放器少发 4% | 少发 **0.90%** [0.08, 1.72],不是 4% |
| **后坐力回复**:游戏把视角往回拉,所以不补偿那臂读低 | 要求偏差随位移成比例,而 85 倍的位移跨度上没有趋势 |
| **`K` 上下不对称 6.32%,1.5474 是两个方向的混合** | 干净测出来 **0.19%**。那个 6.32% 来自 n=3、同一个 run、还过了混叠还原 |
| **相关器每对超读一个固定的 ~0.04 px,一梭因此高 7.5%** | 那是曲线在 **δ≈0.6** 那一端的值,不是常数。一梭的 δ 横跨零点,净值 **−0.28% / −0.81%**。见二之一 |
| **`eps ≈ 0`,命令多少就到多少** | 那是 ±10% 扫描的**零点**(测不动点,回路会把它推到 1),不是**斜率**(测交付率) |

⚠ **有一条从这张表里出去了,而它出去的方式值得记:「相关器偏差随每帧位移变,所以 K 在梭的工况下不同」。** 它当时被判假的理由是「尺寸对、方向反」。**前半句现在是复现过的真话**(二之一:`b(δ)` 确实随 δ 变,而且变号),假的只是后半句给 `K` 推出的那个后果——一梭的净效应在 1% 以内,而不是它想要的方向和幅度。

> **一个「机制成立、后果不成立」的假设,整条记成「假的」会在下一次挡住那个机制。** 这张表以后拆成两半写:机制一行,后果一行。

### 闸门缺的那一道 —— **补上了一道,还缺一道**

⚠ **`η` 从「模型最强的一句话」到撤回,中间只隔了一次复现,而当时没有任何一道闸查这件事。**

**现在有一道了,但只覆盖这一个探针**:`probe_correlator_bias.py --save` / `--replicate` 把两个 run 并排报,单跑会打印 `⚠ ONE RUN`。它当场就赚回来了——`b(δ)` 的**形状**复现了,`K_eff` 的**标高**跨 run 挪 1.08%,而两件事在单个 run 的输出里长得一模一样。

⚠ **`collect_timed` → `fit_time_curve` 那条主路仍然没有。** 一次拟合吃的是全库,而全库跨了几十个场次,所以「复现」在那条路上要问的不是「再跑一次一不一样」,而是**「按场次切开,每一片给的曲线一不一样」**——那是同一条规矩在池化数据上的形状,还没有实现。

**一个只在一个 run 里成立的效应,不进 `config.py`,不进这份文件的正文,只进上面那张表。**
