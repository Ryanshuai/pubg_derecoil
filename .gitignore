for_data_check
for_database
temp_image
.idea
__pycache__
*.pyc
*.xml
*.iml
*.jpg
*.png
*.jpeg
*.log
*.webm
*.mp4
*.mkv
temp_debug
training_data
InGameScreenshot
docs

# RT-DETR/YOLO weights, 189 MB each. Downloaded or trained, never edited.
human_detection/*.pt

# (weapon_hud_bank.npz needs no rule of its own — it lives under training_data/,
#  which line 17 already covers. Rebuild it with tools/build_weapon_hud_bank.py.)


.claude/settings.local.json
.pixi/

pico_firmware/.vscode


# agent-refactor skill 的状态与产物（队列、可达图、扫描结果、差分 spec）
.agent-refactor/
